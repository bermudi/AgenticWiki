---
title: Model Filing Evaluations
created: 2026-07-23
updated: 2026-08-02
type: meta
---

# Model Filing Evaluations

A registry of model filing runs. Use it to compare how different models perform at the AgenticWiki pipeline and to decide whether skills or steps need to change.

## Summary

| Date | Provider / Model | Source(s) | Verdict | Output | Process | Key Issues | Pipeline Changes Suggested |
|---|---|---|---|---|---|---|---|
| 2026-07-23 | poe-completions / hy3-n | arXiv 2607.13285 — *Harness Handbook* (PDF) | Competent; output A-, process B | Strong concept/author pages; accurate source fidelity; clean `validate-page`; good integration | Read skills and ran gates, but mishandled `delegate`/worker separation; theory-review subagents went off-script | Stale `updated` dates on `self-harness.md` and `harnessx.md`; over-sourced `code-as-agent-harness.md`; high token/time cost | Final frontmatter sweep on all touched pages; add `delegate` schema sanity check; tighten theory-review prompt |
| 2026-07-26 | poe-completions / hy3-n | Boundary "AI that Works" livestream — *Building a Shared Memory System for AI Coding Agents* (YouTube, multi-speaker) | Strong output, flawed process; output A, process C+ | Excellent new concept page + 8 clean connective edits; multi-speaker attribution handled honestly; all reviewer warnings addressed | Same `delegate` schema mismatch (2 failed calls); theory-review subagent went off-script AGAIN and its claimed fixes were silently lost (isolated filesystem) | Theory-reviewer's "fixes" to 8 unrelated GraphRAG pages never landed in repo; model trusted self-report; ~62 min / ~8M tokens, much discarded | Document `delegate` filesystem isolation; fix recurring `delegate` schema bug; re-scope theory-reviewer to forbid unrelated edits; implement the 2026-07-23 prompt-tightening recommendation that was not applied |
| 2026-07-26 | command-code / mixed (MiniMax-M3 → GLM-5.2 → MiniMax-M3) | GitHub `FareedKhan-dev/all-agentic-architectures` — README + deterministic-picker tutorial + benchmarks (3 web sources, SHA-pinned) | Strong output, flawed process; output A, process C | Clean new concept + project pages; honest 8-vs-9 discrepancy handling; 33/42 benchmark arithmetic independently verified; all claims trace to raw | Role separation actually achieved (writer dispatched as subagent); no `delegate` schema bug; raw preservation clean; but verifier hallucination misdiagnosed as script bug, and 3 bogus debt entries proposed | Verifier subagent fabricated "missing YAML frontmatter on all 9 paths"; agent misdiagnosed as script bug instead of LLM hallucination; over-deferred 3 debt rows (2 based on invented conventions, 1 self-contradicted); required human takeover to commit; ~2h active / ~6h wall (4h usage-limit gap), 2.4M input tokens, $1.71 | Add "verify the verifier" step (re-run deterministic check before diagnosing cause); tighten debt-registration discipline (verify issue is real, not trivially fixable, not self-contradicted before recording); don't defer one-line mechanical fixes as debt |
| 2026-07-27 | poe-completions / hy3-n | Matt Pocock YouTube — *"Don't waste time on specs: /prototype instead"* (single-speaker) | Strong output, improved process; output A- (revised from A), process B+ | Exemplary new concept page + 6 clean connective edits; theory reviewer ran cleanly for the first time on hy3-n; held commit gate (conservative — skill says commit on PASS) | `delegate` schema bug recurred (3rd consecutive run); skipped diff + quality reviewers; `git add -A` used twice; coordinator+writer collapsed | 4 wiki-voice slips in `prototyping.md` missed by both the source-fidelity reviewer and the evaluator's first pass (behavior-under-load inflation, 2-3 candidates hedge, "negligible vs wrong spec" attribution, "dominant case/hardest" escalation); `delegate` first call failed; 2 of 4 substantive reviewers not dispatched; `git add -A`; ~39m wall, ~1.08M tokens | Implement the `delegate` schema sanity check (3 runs, 3 failures); add a reviewer-dispatch checklist to `coordinating-filing`; forbid `git add -A`; source-fidelity reviewer needs a wiki-voice-vs-paraphrase check (not just cited-quote accuracy) |
| 2026-07-27 | opencode / mimo-v2.5-free | Frank Coyle YouTube — *"Why Agentic Systems Need Ontologies"* (single-speaker, AI Engineer talk) | Strong output, strong process; output A, process B+ | Clean new concept + author pages; 2 additive connective edits with correct synthesis callouts; all 13 cited claims independently verified against transcript with zero wiki-voice slips; all 4 substantive reviewers dispatched (first run in registry to achieve this); proactive broken-link cleanup | Role separation achieved (writer dispatched as subagent); theory reviewer ran cleanly; no `git add -A`; committed on PASS (correct per skill); `delegate` schema bug recurred (4th run, new variant: named a non-existent agent); `edit` schema bug in writer subagent (same class, different tool); theory reviewer's non-blocking suggestion not applied | `delegate` first call failed with "Unknown agent(s): filing-agentic-sources"; writer hit `edits.0: must be object` twice; `[[the-verifiability-thesis]]` connection missed on neuro-symbolic-ai.md; ~19m wall (fastest in registry at the time), ~3.7M tokens | Implement schema sanity check across `delegate` AND `edit` (4 runs, 2 tools); route non-blocking theory-reviewer suggestions back to writer when they add meaningful connections |
| 2026-07-27 | opencode / mimo-v2.5-free | Benoit Schillings YouTube — *"Software engineering is not about writing code"* (single-speaker, AI Engineer talk) | Strong output, strong process; output A-, process B+ | Clean new author + 2 concept pages; 4 additive connective edits; all key quotes verified against transcript; legitimate debt registration (widening-gap extension); first run in registry with zero schema bugs | Role separation achieved; theory reviewer ran cleanly (3rd consecutive); no `git add -A`; zero `delegate`/`edit` schema bugs (first run — breaks 4-run streak); committed on PASS (correct per skill); but verifier ran 3 reviewers inline instead of dispatching isolated workers (regression from Coyle) | 2 minor wiki-voice slips ("context window" for Schillings' "context"; "for the foreseeable future" escalates "at least in the near future") missed by inline source-fidelity review, both fixed post-evaluation; verifier timestamp citations imprecise (VP title cited [1:16], actual [0:16]); verifier collapsed diff/source-fidelity/quality into one context; ~15m wall (fastest in registry), ~6.7M tokens | Enforce reviewer isolation in `verifying-wiki-changes` (verifier must dispatch, not perform inline); extend source-fidelity to check paraphrase fidelity (still open from Pocock run — the "context window" slip is the same class) |
| 2026-07-27 | freebuff / minimax-m3 | Tariq Shaukat YouTube — *"In the Land of AI Agents, the Verifiers Are King"* (single-speaker, AI Engineer talk) | Solid output, partially-visible process; output A-, process B (visibility-limited) | Clean new author + concept pages; semantic Synthesis callout tying AC/DC to [[verifiability]]; 3 additive connective edits with correct attribution; update/unaudited_marginal hygiene; direct quotes accurate on first pass | Transcript only captures initial setup (skill load + 3 file reads of ~824KB) — process visibility limited to what the artifact reveals; clean committed result is *consistent with* correct pass-through of the gates but cannot prove it; correct commit on PASS; no schema bugs visible; `validate-page` clean | 1 mild wiki-voice conflation in `wiki/authors/tariq-shaukat.md` Key Claims section (30% token reduction + clean-codebase compounding joined into one sentence, conflating two empirical claims Shaukat makes separately); one could also flag "constraint is under-discussed relative to context" as a light inflation of the source's "I don't think this part is talked enough about" | Future evaluations should preserve full session transcripts (not just inline tool outputs) so process discipline can be graded without inferring from artifact-only signals; consider recording process aside from the under-the-hood transcript snap to a `.agents/logs/<run-id>.jsonl`-style artifact
| 2026-08-02 | freebuff / minimax-m3 | Harrison Chase YouTube — *"The Agent Development Lifecycle 101"* (single-speaker, LangChain webinar) | Strong output, strong process; output A, process B+ | 5 clean new pages (author + 3 concepts + project) with correct epistemic callouts; 7 additive connective edits; zero wiki-voice slips on independent first pass (2nd clean first pass after Coyle); all 4 review classes dispatched as isolated subagents; zero schema bugs (2nd consecutive); no `git add -A`; correct commit on PASS; reviewer polish items applied pre-commit | Coordinator+writer collapse (opus-agent/gpt-5-agent unavailable; agent overrode skill's "stop and warn" to do writer work inline — defensible but a deviation); reviewer subagents had NO TOOLS — couldn't independently read files, relied on conversation context only (theory reviewer couldn't re-read threads it was asked to review); 1 non-blocking diff-reviewer observation unapplied | Reviewer isolation without read access defeats the theory reviewer's purpose; coordinator+writer collapse is environment-driven; ~27m wall, full transcript preserved (1.4MB) — first freebuff run with complete process visibility | Ensure reviewer subagents have file-read tools (isolation without read access is partial); document/authorize the inline-writer fallback when no writer agent is spawnable, with mandatory user disclosure; paraphrase-fidelity check still open but this run had zero slips |

## 2026-07-23 — poe-completions / hy3-n — arXiv 2607.13285

### Source & Session

- **Source:** arXiv 2607.13285 — *Harness Handbook: Making Evolving Agent Harnesses Readable, Navigable, and Editable* (ingested from a local PDF)
- **Session log:** `/home/daniel/.pi/agent/sessions/--home-daniel-Documents-AgenticWiki--/2026-07-23T06-35-15-521Z_019f8daf-7ac1-7a1e-81c5-52f82793c50c.jsonl`

### Verdict

Competent. The final artifact is good; the path to it was bumpier and more expensive than necessary.

### Output Quality

- Created clean new pages: `wiki/concepts/harness-handbook.md` and `wiki/authors/ruhan-wang.md`.
- Empirical claims (win rates, token costs, F1 gains, 6 vs 2,267 source files, SKILL.md manifest, BGPD workflow) verified against `raw/2607.13285v1.md`.
- Good integration: update callout on `harness-engineering.md`, reciprocal links on `code-as-agent-harness.md`, `self-harness.md`, `harnessx.md`, and `index.md` updated.
- `> [!note] Synthesis` correctly attributes the localization-before-evolution frame shift to the wiki author.
- Final `validate-page` run on the changed scope was clean.

### Process Discipline

- Read `coordinating-filing`, `filing-agentic-sources`, `meta/wiki-conventions.md`, and `references/ingest-philosophy.md` before acting.
- Did not cleanly separate coordinator and writer (single-agent environment, so it performed both roles).
- Failed the first `delegate` call due to schema mismatch; theory-review subagents went off-script — one ran an 18-minute full-wiki audit, the second lost context — so the model performed the theory gate inline.
- Recovered by dispatching diff / source-fidelity / quality reviewers via `delegate` and applying their findings.

### Issues Found

1. **Stale `updated` dates.** `wiki/concepts/self-harness.md` and `wiki/concepts/harnessx.md` were edited to add reciprocal `[[harness-handbook]]` links, but their frontmatter `updated` dates were not bumped from `2026-07-04`.
2. **Inconsistent sourcing.** `raw/2607.13285v1.md` was added to `wiki/concepts/code-as-agent-harness.md` sources/frontmatter for Related-link-only additions, while `self-harness.md` and `harnessx.md` were not — even though all three received reciprocal links.
3. **Efficiency.** ~23 minutes wall time and several million tokens, mostly from misdirected subagent runs.

### Recommended Pipeline Changes

1. Add a final frontmatter sweep that checks `updated` on **all** pages touched after the last reviewer feedback, not only the pages reviewers explicitly flagged.
2. Add a `delegate` tool/schema sanity check at the start of the pipeline, or document a single-agent fallback when subagents are unavailable.
3. Tighten the `reviewing-wiki-theory` prompt to require the exact output format and add an inline fallback when a subagent drifts.

### Post-Evaluation State

- The stale `updated` dates were fixed by the evaluator after the log was committed, then this note was updated to match the live wiki.

## 2026-07-26 — poe-completions / hy3-n — Boundary livestream (context shards)

### Source & Session

- **Source:** Boundary "AI that Works" livestream — *"Building a Shared Memory System for AI Coding Agents"* (YouTube, multi-speaker: hosts Live + Dex [Dex Horthy, HumanLayer]; guest Vibe [Vibv, BAML/Boundary]). Ingested from a local downloaded transcript.
- **Session log:** `/home/daniel/.pi/agent/sessions/--home-daniel-Documents-AgenticWiki--/2026-07-26T02-29-08-019Z_019f9c41-3933-7e3f-805b-b58260437653.jsonl`
- **Commit:** `4bc2f3c` — 11 files changed, 2 new (`raw/boundary-context-shards-shared-memory.md`, `wiki/concepts/context-shards.md`).
- **Wall time:** ~62 minutes (02:29:08 → 03:31:38 UTC). **Subagent cost:** ~7.8M tokens across 4 delegate tasks (theory review alone: 6.5M tokens / 24m52s).

### Verdict

Strong artifact, bumpier process than the 2026-07-23 run. The output is the best of the two runs; the process regressed on a known failure mode.

### Output Quality

- **New concept page `wiki/concepts/context-shards.md`** is exemplary in structure: Problem → Volume-based philosophy → Design (extraction, data model, team aggregation, staged HITL, injection, decay) → relationship to wiki memory theory → anti-slop mechanism → Thread → Related → Sources. Epistemic callouts (`Departure` / `Synthesis` / two `Extension`s) are placed at claim level and explicitly framed as wiki voice vs. product opinion.
- **Connective edits across 8 pages** are clean and additive: `agent-memory-systems`, `dreaming`, `the-slop-problem`, `construction-time-memory` (thread), `humanlayer`, `baml`, `vibv`, `dex-horthy`, `index.md`. Every touched page bumped `updated` to 2026-07-25; every `sources:` list *added* the new raw without dropping priors; `unaudited_marginal` correctly reset to 0 (this was a full ingest, not marginal).
- **Multi-speaker attribution is handled honestly.** The model added an explicit caveat on every page that uses quotes: the transcript carries `[mm:ss]` timestamps but no per-line speaker labels, so quotes are attributed to "the discussion/video" rather than to named individuals. The `speakers:` list in the raw frontmatter was repaired (it was missing `type`/`url`/`title`/`channel`/`date_saved`/`speakers`).
- **The "ingestion-time twin of dreaming" synthesis is coherent** and maps cleanly onto the existing M=⟨R,S,Q,U⟩ / in-band-out-of-band theory. The theory reviewer (once it finally reported) agreed: no panorama reframe, no thread-level tension.
- **De-duplication was legitimate.** The diff reviewer flagged a `> [!note] Synthesis:` callout on `dreaming.md` being replaced by a new `> [!note] Extension:` about context shards. Verification: the removed prose is preserved verbatim on its canonical home `wiki/concepts/episodic-memory-for-agents.md`, the cross-link is retained in both body and Related, and the model added an explicit pointer at the end of the new callout ("For the CLS consolidation / default-mode after-action realization of dreaming, see [[episodic-memory-for-agents]]"). No knowledge lost.
- **`validate-page` on the changed set: WITHIN_BUDGET** (0 errors, 5 informational notes about pre-existing merged-redirect stubs).

### Process Discipline

- Read `coordinating-filing`, `filing-agentic-sources`, `meta/wiki-conventions.md`, and `references/ingest-philosophy.md` before acting.
- Did not cleanly separate coordinator and writer (single-agent environment — same as the prior run).
- **`delegate` schema mismatch recurred.** The first theory-review delegate call and the first diff/source/quality batch both failed validation with `tasks.0: must be object` because the model passed `tasks` as a JSON *string* instead of an array of objects. The model recovered on retry in both cases. This is the **second consecutive run** with this exact failure — the 2026-07-23 evaluation already recommended "add a `delegate` schema sanity check"; that recommendation was not implemented.
- **Theory-review subagent went off-script AGAIN.** Instead of producing a theory-summary report, it ran a 24m52s full-wiki audit (6.5M tokens), "fixing" 12 backslash-corrupted wikilinks and 8 missing `unaudited_marginal` fields on pages **entirely unrelated to this ingest** (GraphRAG cluster, `ai-design-loop`, `failure-modes`, `self-conditioning`). The 2026-07-23 evaluation already recommended "tighten the `reviewing-wiki-theory` prompt"; that recommendation was not implemented either.
- Diff / source-fidelity / quality reviewers ran correctly in a single parallel batch (3/3 tasks, 5m15s total).
- **Source-fidelity reviewer caught a CRITICAL:** the transcript has no speaker labels, so the model removed two named-individual quote attributions and corrected Vibv's role from "co-host" to "guest" (matching the raw frontmatter) across `baml`/`vibv`/`humanlayer`/`context-shards`.
- **Quality reviewer caught 5 warnings** — all addressed before commit: `dex-horthy`→`context-shards` originator backlink added, `index.md` `updated` date bumped, Vibv role reconciled across pages, `vibv.md` author→concept backlink reframed, `construction-time-memory` reciprocal link added.

### Issues Found

1. **CRITICAL PROCESS BUG — subagent edits silently lost.** The theory-review subagent reported "Fixed via `sed`" for 12 broken links and "Fixed by inserting `unaudited_marginal: 0`" for 8 GraphRAG pages. Verification against the live repo: **all 8 GraphRAG pages still lack `unaudited_marginal`** (`graphrag-logical-inconsistency`, `graphrag-structural-fragmentation`, `graphrag-thematic-irrelevance`, `memgraphrag`, `memory-based-multi-agent-system`, `personalized-pagerank-rag`, `three-layer-global-memory`, plus the `graphrag-quality-problems` thread). The `delegate` tool returns only text output — subagents run in an isolated filesystem and their file edits do **not** propagate to the main session. The model trusted the subagent's self-reported "fixed" status, surfaced "theory review PASS" in its summary, and committed. The claimed fixes were never applied. This is a false-verification pattern: a reviewer that "fixes" things in a sandbox it cannot write through produces a misleading PASS.
2. **Recurring `delegate` schema bug.** Two failed delegate calls this run; same failure mode as 2026-07-23. The model has not internalized the correct argument shape and the prior recommendation was not acted on.
3. **Recurring theory-reviewer drift.** Second consecutive run where the theory reviewer ran a full audit instead of a theory review. The prior prompt-tightening recommendation was not applied.
4. **Efficiency.** ~62 minutes wall, ~8M+ tokens. The theory-review subagent alone consumed 6.5M tokens / 25 minutes on work that was (a) out of scope and (b) discarded. The actual ingest + connective edits + three valid reviewer runs fit comfortably in the remaining ~37 minutes.

### Recommended Pipeline Changes

1. **Document `delegate` filesystem isolation in `coordinating-filing`.** Subagent file edits do not propagate. The coordinator must either (a) re-apply claimed fixes itself after receiving the report, or (b) instruct subagents to return unified diffs and apply them in the main session. **Never trust a subagent's self-reported "fixed" status as verification.** This is the most important change — it closes the false-verification hole.
2. **Fix the recurring `delegate` schema bug.** Either change the tool schema to accept a JSON string, or paste the exact argument shape (array of objects, not a stringified array) into `coordinating-filing` as a worked example. Two consecutive runs failing the same way is a skill problem, not a model problem.
3. **Re-scope `reviewing-wiki-theory` to forbid unrelated edits.** A theory reviewer that "fixes" 12 broken links on pages outside the changeset is no longer a theory reviewer — it is an auditor. Either (a) add a hard "DO NOT EDIT; if you find unrelated issues, list them in the report under 'Out-of-scope observations' and stop" rule, or (b) split the role into a theory reviewer (read-only) and a separate auditor (write-capable, explicitly invoked). Implement the 2026-07-23 prompt-tightening recommendation that was not applied.
4. **Track recommendation follow-through.** Two of the three 2026-07-23 recommendations were not applied and the same failures recurred. Consider a `meta/pipeline-recommendations.md` ledger with open/closed status, or fold the open items into `meta/tech-debt.md`.

### Post-Evaluation State

- The 8 GraphRAG pages missing `unaudited_marginal` were fixed by the evaluator after this evaluation was written (`unaudited_marginal: 0` inserted per `meta/wiki-conventions.md`), then this note was updated to match the live wiki. `validate-page` on the 8 pages: WITHIN_BUDGET.
- A repo-wide scan then surfaced 8 more pages missing the field (7 author pages — `discover-ai`, `qinggang-zhang`, `jinsong-su`, `chuanjie-wu`, `zhishang-xiang`, `yunbo-tang`, `zerui-chen` — and 1 project page — `memgraphrag-project`); all pre-existing, unrelated to this ingest. Fixed in the same pass. **Repo-wide scan now reports 0 pages missing `unaudited_marginal`.**
- The ingest itself (commit `4bc2f3c`) is sound and needs no rework.

## 2026-07-26 — command-code / mixed (MiniMax-M3 → GLM-5.2 → MiniMax-M3) — all-agentic-architectures

### Source & Session

- **Source:** GitHub `FareedKhan-dev/all-agentic-architectures` (HEAD `cf9d620a8cc55d59589399c30f305e6dfaa428ec`, 2026-05-28). Three web sources preserved as `raw/all-agentic-architectures-{readme,deterministic-picker,benchmarks}.md`, each with a SHA-pinned `url` field per the wiki's GitHub-repo source convention.
- **Session log:** `/home/daniel/Documents/AgenticWiki/command-code-session-c98a2b9f.jsonl`
- **Commit:** `e71a4ce` — 9 files changed, +1118 / -4 (3 raw, 2 new wiki pages, 3 updated threads, 1 index update). Committed by the evaluator after taking over the gate (see Process Discipline).
- **Wall time:** ~6h14m (18:50 → 01:04 next day), but ~4h of that was a usage-limit gap (19:43 → 00:22). **Active work time: ~2h.**
- **Cost:** 2.4M input tokens, 34K output tokens, 1.6M cache-read tokens, **$1.71**. 38 assistant turns, 7 subagent dispatches.
- **Models:** Mixed — MiniMax-M3 (start) → GLM-5.2 (middle, 18:59–19:32) → MiniMax-M3 (end, 19:32 onward). The model change happened mid-pipeline, between plan presentation and writer dispatch.

### Verdict

Strong artifact, flawed process. The output is the cleanest of the three runs filed here; the process regressed on a new failure mode (verifier hallucination misdiagnosed as script bug) and introduced a new one (over-deferred debt).

### Output Quality

- **New `wiki/concepts/deterministic-picker.md`** is well-shaped: Problem (flat-band pathology) → Fix (categorical commitment + Python composition) → Coverage (13 of 35 architectures, with the 8-vs-9 discrepancy handled honestly) → Relation to constructible verifiability → Boundaries → Thread → Related → Sources. Attribution is consistent ("the repo author documents", "Per the author") and wiki synthesis is explicitly marked in three places.
- **New `wiki/projects/all-agentic-architectures.md`** carries the 35-architecture taxonomy, the 8-family structure, the 9-provider matrix, the 33/42 benchmark result, and the named pattern-fit failures. The 35-vs-36 row-count discrepancy (BrowserAgent/ComputerUse split) is flagged with a `[!note]` callout. The "0 MOCKED RUNS" hero metric is correctly framed as a freshness signal, not a validation signal.
- **Three thread updates are additive and well-attributed:** `agent-quality-engineering` (deterministic-picker as a more aggressive hybrid on the Horthy/Galarza axis), `the-multi-agent-theory` (a Departure callout for the practitioner benchmark, honestly hedged as "single-author, single-model, 17-task, not a rigorous audit"), `the-verifiability-thesis` (an Extension callout positioning the picker as constructible verifiability at the scorer step).
- **Independent verification by the evaluator** (separate from the in-session reviewers): every factual claim was cross-checked against the three raw sources. The 33/42 benchmark arithmetic was independently re-derived by summing all 36 leaderboard rows (24×1 + 3×2 + 1×3 = 33 numerator; 31×1 + 4×2 + 1×3 = 42 denominator). The 4-booleans-plus-1-bounded-integer paraphrase is correct against the code (the tutorial's own "5 independent booleans" phrasing is wrong; the wiki silently corrects it). No invented quotes, no wiki-voice violations, no misattributions found.
- **`validate-page` on the changed scope: WITHIN_BUDGET** (0 errors, 0 warnings, 5 informational notes about pre-existing pages unrelated to this ingest).

### Process Discipline

- Read `coordinating-filing`, `filing-agentic-sources`, `meta/wiki-conventions.md`, and `references/ingest-philosophy.md` before acting.
- **Role separation actually achieved** — this is the first of the three runs that dispatched the writer as an isolated subagent rather than collapsing coordinator + writer into one context. The writer was dispatched twice (initial ingest, then a fixup pass for the theory-gate findings, then a final fixup for the source-fidelity CRITICAL).
- **Raw preservation clean** — three SHA-pinned URLs, slugified filenames, provenance frontmatter (`type: web`, `url`, `title`, `author`, `date`, `ingested`). No `git add -A`; staging boundary was the union of writer-reported wiki paths and coordinator-staged raw paths, exactly per skill.
- **Theory gate ran correctly** — returned PASS WITH WARNINGS, the agent applied 4 surgical fixes (Galarza imprecision, Layer-4 softening, Sally-Anne rename, 9-immune sourcing note) via a writer re-dispatch, then re-ran the mechanical pre-check. Correct loop.
- **No `delegate` schema bug** — this run used the `agent` tool, not `delegate`, so the recurring `tasks.0: must be object` failure from the two prior runs did not occur.
- **CRITICAL PROCESS BUG — verifier hallucination misdiagnosed as script bug.** The `verifying-wiki-changes` subagent reported "missing YAML frontmatter on all 9 paths" and stopped without constructing the three substantive reviewers. The filing agent re-ran `validate-page` directly and confirmed all 9 paths were `WITHIN_BUDGET` — so it caught the hallucination in real time. But it misdiagnosed the cause as "a bug in the verifying scripts" and said it would "work around the verifier bug" by dispatching the three reviewers directly. The skill itself warns that "LLM verdicts flag where to look… not ground truth" — the agent should have concluded "the verifier subagent hallucinated," not "the script is buggy." The misdiagnosis was corrected only after the evaluator intervened and pointed out that (a) `validate-page` takes positional path arguments only, (b) there is no `--help`/`--changed` flag, (c) a bad arg would produce "path not found," not "missing YAML frontmatter," and (d) the "missing YAML frontmatter" error requires the file to exist and be readable but not start with `---` — a different code path entirely. The agent accepted the correction and proceeded correctly.
- **NEW PROCESS BUG — over-deferred debt.** After the three substantive reviewers returned PASS / PASS WITH WARNINGS / PASS WITH WARNINGS, the agent proposed three debt entries for `meta/tech-debt.md`:
  1. "every major agentic AI pattern" attribution wording — **bogus**: the README says it verbatim (line 67) and the wiki page attributes it explicitly with "the README describes as packaging every major..."
  2. `index.md` entry appended at end, not alphabetical — **bogus**: the Projects & Tools section is not alphabetical (it's topically grouped); the wiki-quality reviewer invented a convention that doesn't exist.
  3. Project page missing `## Thread` section — **bogus**: the agent itself admitted it's "consistent with the closest existing analog (`memgraphrag-project.md`)." A style choice that matches the nearest analog is not debt.
  All three were correctly rejected by the evaluator. The agent had aggregated the verdict as `PASS WITH EXPLICIT DEBT` when the correct aggregation was `PASS`. This is a mild but real over-defer pattern: recording non-debt as debt pollutes `meta/tech-debt.md` and makes future audits harder.
- **Stuck at commit gate, required human takeover.** The agent held the gate (more conservative than the skill requires — the skill says "PASS: commit the staged set," but the agent held anyway) and proposed three bogus debt entries alongside the hold. The evaluator took over, verified the changeset independently, rejected all three debt entries, and committed as `e71a4ce` with no debt rows written.

### Issues Found

1. **CRITICAL — Verifier hallucination misdiagnosed.** The `verifying-wiki-changes` subagent fabricated "missing YAML frontmatter on all 9 paths" output. The filing agent caught the hallucination (re-ran `validate-page` directly, got WITHIN_BUDGET) but misdiagnosed it as a script bug rather than recognizing it as LLM unreliability. The skill's own warning ("LLM verdicts flag where to look… not ground truth") was not internalized. Corrected only by evaluator intervention.
2. **Over-deferred debt.** Three debt entries proposed when zero were warranted. Two were based on conventions the reviewer invented (alphabetization); one was self-contradicted by the agent's own observation (matches the closest analog). This is a new failure mode not seen in the prior two runs.
3. **Required human takeover to commit.** The agent held the gate correctly, but the gate hold was coupled to bogus debt entries. The evaluator had to take over, reject the debt, and commit. Without intervention, the agent would have either (a) committed with three bogus debt rows or (b) continued holding indefinitely.
4. **Mixed-model run.** The model changed mid-pipeline (MiniMax-M3 → GLM-5.2 → MiniMax-M3). The GLM-5.2 segment handled the plan presentation and writer dispatch; the final MiniMax-M3 segment handled verification and the commit-gate hold. This is not necessarily a problem, but it makes per-model process attribution harder.

### Recommended Pipeline Changes

1. **Add a "verify the verifier" step to `coordinating-filing`.** When a coordinator subagent (verifier, theory reviewer, or any reviewer) reports a failure, the coordinator must independently re-run the relevant deterministic check *before* diagnosing the cause. Concrete: if the verifier claims "missing YAML frontmatter on path X," the coordinator runs `./scripts/validate-page X` directly. If the deterministic check passes, the verifier hallucinated; do not record as script debt, do not mention a "script bug" in any commit message or debt row, and re-dispatch the verifier with a note that its prior output was inconsistent with the deterministic check. This operationalizes the skill's existing warning ("LLM verdicts flag where to look… not ground truth") and closes the misdiagnosis hole.
2. **Tighten debt-registration discipline.** Before recording a debt row, the coordinator must verify three things: (a) the issue is real (check the convention against the actual wiki — don't take a reviewer's claim of "should be alphabetical" at face value), (b) the issue is not trivially fixable (one-line mechanical fixes get fixed, not deferred), (c) the issue is not self-contradicted by the agent's own observations ("consistent with the closest analog" disqualifies the row). Add this as a checklist item to the commit-gate section of `coordinating-filing`.
3. **Don't defer fixable warnings as debt.** If a reviewer warning is a one-line mechanical edit (e.g., moving an index entry, bumping a date, adding a `## Thread` section), fix it via a writer dispatch and re-run only the affected check. Debt is for genuine deferrals — claims that need external research, evidence gaps that need a new source, or schema decisions that need human approval — not for "I don't want to run another reviewer cycle."
4. **Implement the prior recommendations.** Two of the three 2026-07-23 recommendations (delegate schema sanity check, theory-reviewer prompt tightening) were not applied and the same failures recurred in the 2026-07-26 Boundary run. This run used a different subagent mechanism (`agent` not `delegate`) so the schema bug did not recur, but the theory-reviewer drift was not tested (the theory reviewer ran cleanly this time). The recommendation-tracking ledger from the 2026-07-26 evaluation is still open.

### Post-Evaluation State

- The evaluator took over the commit gate, independently verified every factual claim against the three raw sources (including re-deriving the 33/42 benchmark arithmetic), rejected all three proposed debt entries as bogus, and committed as `e71a4ce` with no debt rows written.
- The ingest itself (commit `e71a4ce`) is sound and needs no rework. The verifier-hallucination and over-deferred-debt findings are process issues for future runs, not content issues with this changeset.

## 2026-07-27 — poe-completions / hy3-n — Matt Pocock `/prototype`

### Source & Session

- **Source:** Matt Pocock YouTube video — *"Don't waste time on specs: /prototype instead"* (single-speaker, ~11 minutes). Ingested from a local downloaded transcript in `~/Downloads`.
- **Session log:** `/home/daniel/.pi/agent/sessions/--home-daniel-Documents-AgenticWiki--/2026-07-27T02-03-31-462Z_019fa150-2305-7c8a-8766-0fb085e99f63.jsonl`
- **Commit:** not committed by the agent (held the gate — more conservative than the skill requires; the skill says "PASS: commit the staged set"). Staged changeset: 8 files, +756 / -12 (1 raw, 1 new concept, 6 connective edits, 1 index update).
- **Wall time:** ~39 minutes (02:03:31 → 02:42:49 UTC). **Token cost:** ~1.08M total — main session ~107k, theory reviewer 832k (3m28s), source-fidelity reviewer 137k (4m26s). 38 assistant turns, 2 delegate dispatches (1 failed, 2 succeeded).
- **Models:** Started on `zai/glm-5.2` for ~5 seconds (no work produced), then switched to `poe-completions/hy3-n` for the entire run. Effectively a single-model hy3-n run.

### Verdict

Strong artifact, cleanest process of the three hy3-n runs filed here — but the initial A grade on output was revised to A- after a second pass found 4 wiki-voice/source-fidelity slips in `prototyping.md` that both the in-session source-fidelity reviewer and the evaluator's first pass missed. The process is still the best of the three hy3-n runs (theory reviewer stayed in scope, commit gate held, no bogus debt), but two known failure modes recurred (the `delegate` schema bug and the coordinator+writer collapse) and the skipped diff + quality reviewers had a concrete content cost: the wiki-voice slips they would have caught went uncaught.

### Output Quality

- **New concept page `wiki/concepts/prototyping.md`** is well-shaped: abstract → Body → Fidelity Framework (low vs high) → Prototype Skill (mechanics) → UI vs Logic Prototypes → Wayfinder → Relationship to Existing Theory → Thread → Related → Sources. Epistemic callouts are correctly typed and placed: `Synthesis` on `mattpocock-skills.md` (Wayfinder as upstream scheduler), `Extension` on `intent-to-code` (prototyping raises alignment-first to higher fidelity), `Marginal` on `the-agent-workflow` (the grilling → prototype → AFK re-implement cycle). Wiki voice vs. Pocock opinion is distinguished throughout.
- **Six connective edits are additive and well-attributed:** `intent-to-code`, `the-agent-workflow`, `ai-design-loop`, `mattpocock-skills`, `matt-pocock`, `index.md`. Every touched page bumped `updated` to 2026-07-27 (the issue that tripped the 2026-07-23 run is handled correctly here). Every `sources:` frontmatter list adds the new raw without dropping priors; every `## Sources` body list gets a corresponding annotation. `unaudited_marginal: 0` on the new page (correct for a full ingest).
- **Independent verification by the evaluator** (separate from the in-session reviewers): every key empirical claim was cross-checked directly against `raw/yt-dont-waste-time-on-specs-prototype-instead.md` at the cited timestamps — the ~100k-token cost (`[5:46-5:50]`), the 2–3 candidate options A/B/C (`[4:25-4:36]`), the throwaway branch + copy-real-front-end-code handoff (`[6:40-6:50]`), the live-route preference (`[7:15-7:32]`), the logic prototype as a tiny terminal state-machine app (`[9:38-9:46]`), the Shape Up / Ryan Singer lineage (`[10:25-10:35]`), the AFK re-implement-against-spec step (`[8:09-8:19]`), and the Wayfinder grilling/prototype ticket types (`[2:55-3:30]`). All confirmed. No fabricated quotes, no misattribution (solo video — speaker identity is unambiguous).
- **The source-fidelity reviewer's per-claim work checks out** on the claims it checked. I independently spot-checked 8 of its timestamped citations; all are accurate to the transcript. This is the "verify the verifier" step recommended by the 2026-07-26 command-code evaluation — applied here to a reviewer that was in fact reliable on the claims it covered.
- **`validate-page` on the changed set: WITHIN_BUDGET** (0 errors, 0 warnings, 5 pre-existing INFO notes about pages unrelated to this ingest).
- **INITIAL EVALUATION MISSED 4 WIKI-VOICE/SOURCE-FIDELITY ISSUES in `prototyping.md`** (caught on a second pass after the user prompted "no issues to fix?"):
  1. **"behavior-under-load"** (line 58) — inflated Pocock's "how things should behave under certain circumstances" (`[2:11-2:15]`, UI conditional behavior like a modal showing data) into "behavior-under-load" (a performance/load-testing connotation Pocock never invokes). Fixed to "behavior under specific conditions."
  2. **"2–3 candidate implementations"** (line 32) — the transcript shows three options (A/B/C) at `[4:25-4:36]`; Pocock never says "2–3." The hedge was wiki-voice generalization presented as his mechanic. Fixed to "three candidate implementations (options A/B/C in Pocock's walkthrough)."
  3. **"the cost of a prototype is negligible compared to the cost of a wrong spec"** (line 17) — Pocock says code production cost has dropped and prototypes are cheap/effective; he never makes the "negligible vs. wrong spec" comparison. The sentence structure attributed the comparison to him. Fixed to a verbatim quote ("has never been cheaper and never been a more effective tool") instead of the invented framing.
  4. **"the dominant case, because visual/interactive questions are hardest to resolve in discussion"** (line 41) — Pocock says front-end "definitely benefits a lot" (`[8:55-9:08]`), not "the dominant case"; and "really hard" got escalated to "hardest." Fixed to "the case Pocock emphasizes most" + verbatim "really hard to answer during the discussion phase."
  - **Implication for the evaluation:** the in-session source-fidelity reviewer returned PASS and I confirmed its cited claims — but neither of us caught these four wiki-voice slips. The reviewer checked that cited quotes were accurate; it did not catch claims presented as paraphrase that escalated or inflated the source. This is the same class of failure the diff reviewer (`reviewing-wiki-diffs`) and quality reviewer (`reviewing-wiki-quality`) exist to catch, and both were skipped this run. The skipped reviewers are not a victimless omission — this is the concrete content cost.

### Process Discipline

- Read `coordinating-filing`, `filing-agentic-sources`, `meta/wiki-conventions.md`, and the reviewer skills before acting. ✓
- **Did not separate coordinator and writer** — same single-agent collapse as the prior two hy3-n runs. The 2026-07-26 command-code run achieved role separation by dispatching the writer as a subagent; this run regressed to the hy3-n pattern of performing both roles in one context.
- **`delegate` schema bug recurred for the THIRD consecutive hy3-n run.** The first delegate call (theory reviewer) failed validation with `action: must be equal to one of the allowed values` because the agent passed `{"action":"prompt","context":"fresh"}` as top-level arguments instead of a `tasks` array of objects. The agent recovered on retry with `tasks=[{...}]` and the call succeeded. This is the same class of failure documented in the 2026-07-23 and 2026-07-26 Boundary evaluations; both prior recommendations to "add a `delegate` schema sanity check" were not implemented. Three consecutive runs, three failures — this is now the highest-priority open recommendation.
- **Theory reviewer ran cleanly — first time on hy3-n.** Returned PASS in 3m28s / 832k tokens, stayed in scope, produced the correct structured report (threads gained support / took a hit / contradictions / departures / emerging themes / recommended action), and proposed one optional clean-up (the Wayfinder cross-link on `mattpocock-skills.md`) which the agent applied. No off-script full-wiki audit, no fabricated "fixes" to unrelated pages. This is a real improvement over the 2026-07-23 and 2026-07-26 Boundary runs; either the prompt-tightening recommendation finally had an effect, or this smaller changeset gave the reviewer less surface area to drift into.
- **Source-fidelity reviewer ran cleanly** — PASS in 4m26s / 137k tokens, with per-page claim-by-claim verification and timestamp citations. I independently verified its work (see Output Quality above).
- **SKIPPED the diff reviewer (`reviewing-wiki-diffs`).** The agent inspected `git diff --cached` inline itself (one bash call at line 128) but did not dispatch an isolated read-only worker. For substantive edits to existing pages with new callouts (the `Extension` on `intent-to-code`, the `Marginal` on `the-agent-workflow`, the Handoff-row rewrite on `ai-design-loop`), the skill requires transition-integrity review by an isolated worker. The agent's inline diff inspection is not a substitute — it is the coordinator auditing its own writer work, which is the same single-agent conflict the role-separation rule exists to prevent.
- **SKIPPED the quality reviewer (`reviewing-wiki-quality`).** The skill requires this for "a new page, a substantial rewrite or reorganization, or material thread synthesis." This run had all three: a new concept page, a rewritten Handoff row, and two new thread callouts. The reviewer was not dispatched and no justification was given.
- **Used `git add -A` twice** (lines 149 and 153) instead of staging exactly the changeset paths. The worktree was clean before the session (verified at line 12) so no unrelated changes were pulled in, but this is a discipline slip — the skill specifies staging the union of writer-reported wiki paths and coordinator-staged raw paths, not a blanket add.
- **Held the commit gate — more conservative than the skill requires.** Final message: "I did not commit — that gate is yours." The skill says "PASS: commit the staged set," so holding was a conservative choice, not a skill requirement. No bogus debt proposed (the over-defer pattern from the 2026-07-26 command-code run did not recur).
- **Caught and fixed two self-inflicted mechanical errors in real time:** (a) added the new raw to `## Sources` body lists on the two thread pages but forgot the frontmatter `sources:` list — `validate-page` caught it, the agent fixed both; (b) accidentally removed a callout header on `the-agent-workflow.md` during an edit, noticed immediately, and restored it. Good recovery both times.

### Issues Found

1. **`delegate` schema bug — 3rd consecutive run.** First delegate call failed with `action: must be equal to one of the allowed values` (top-level `action`/`context` fields instead of a `tasks` array). Recovered on retry. The 2026-07-23 and 2026-07-26 Boundary evaluations both already recommended a schema sanity check; neither was implemented. Three consecutive failures elevates this from "open recommendation" to "blocking process debt."
2. **Two substantive reviewers skipped.** `reviewing-wiki-diffs` (transition integrity) and `reviewing-wiki-quality` (editorial quality) were not dispatched. The agent performed inline diff inspection itself, which is not the same as an isolated read-only worker — it collapses the reviewer role back into the coordinator+writer single agent. The new page and the two new thread callouts squarely met the skill's "run quality review" threshold.
3. **`git add -A` used instead of explicit path staging.** Twice. No harm this time (clean worktree), but it's the same discipline slip noted in prior evaluations and a future hazard when the worktree is dirty.
4. **Coordinator+writer collapse.** Same as the prior two hy3-n runs. The 2026-07-26 command-code run is the only one of the four that achieved role separation.

### Recommended Pipeline Changes

1. **Implement the `delegate` schema sanity check — now blocking.** Three consecutive hy3-n runs have failed the first `delegate` call with the same root cause (wrong argument shape). Either (a) paste the exact argument shape (`tasks: [{action: "prompt", prompt: "..."}]`) into `coordinating-filing` as a worked example with a one-line "DO NOT pass `action` at the top level" warning, or (b) change the tool schema to accept either shape. The 2026-07-23 and 2026-07-26 Boundary recommendations are still open; this run makes three.
2. **Add a reviewer-dispatch checklist to `coordinating-filing`.** Before the commit gate, the coordinator must explicitly enumerate the four reviews (theory / diff / source-fidelity / quality) and either dispatch each or record a one-line justification for skipping. This run skipped two of four with no recorded justification and the skips had a concrete content cost (see below). Concrete: a 4-row table in the verdict report — `theory: dispatched ✓`, `diff: skipped (mechanical-only changeset)`, `source-fidelity: dispatched ✓`, `quality: skipped (narrow additive)`. If a row is skipped, the justification must cite the risk-classification rule that permits the skip.
3. **Forbid `git add -A` in the staging step.** Replace with explicit path arguments (`git add -- <paths>`). The skill already specifies this; add a one-line mechanical check ("staging command must not contain `-A`") to the pre-commit sweep.
4. **Extend `verifying-source-fidelity` to check wiki-voice-vs-paraphrase, not just cited-quote accuracy.** The source-fidelity reviewer this run verified that every *quoted* claim matched the transcript — and it was correct on every quote it checked. But it missed four claims presented as *paraphrase* that escalated or inflated the source ("behavior-under-load" from "behave under certain circumstances"; "2–3" from "three"; "negligible vs. wrong spec" invented; "dominant/hardest" from "benefits a lot / really hard"). The skill should add an explicit check: for each paraphrased claim, does the source support the *specific framing*, not just the topic? This is the gap between "the quote is accurate" and "the paraphrase is faithful."
5. **Track the open recommendations.** The 2026-07-26 Boundary evaluation proposed a `meta/pipeline-recommendations.md` ledger with open/closed status. It is still open. As of this run: `delegate` schema sanity check (open ×3), theory-reviewer prompt tightening (open but not tested this run — the reviewer ran cleanly), reviewer-dispatch checklist (new), `git add -A` forbid (new), source-fidelity wiki-voice-vs-paraphrase check (new). Consider folding these into `meta/tech-debt.md` if a separate ledger is too much overhead.

### Post-Evaluation State

- The staged changeset was committed as `e54f196` after the first evaluation pass (verdict: output A, process B+).
- **A second pass prompted by the user ("no issues to fix?") found 4 wiki-voice/source-fidelity slips in `prototyping.md` that the first pass missed.** All four were fixed in a follow-up commit. The evaluation entry was updated to revise the output grade from A to A- and to document the misses honestly.
- The 4 fixed issues: (1) "behavior-under-load" → "behavior under specific conditions"; (2) "2–3 candidate implementations" → "three candidate implementations (options A/B/C in Pocock's walkthrough)"; (3) "the cost of a prototype is negligible compared to the cost of a wrong spec" → verbatim quote "has never been cheaper and never been a more effective tool"; (4) "the dominant case, because visual/interactive questions are hardest to resolve in discussion" → "the case Pocock emphasizes most" + verbatim "really hard to answer during the discussion phase."
- The ingest is now sound. The 4 slips are a process finding (the skipped diff + quality reviewers would have caught them) and a source-fidelity-scope finding (the reviewer checked quote accuracy, not paraphrase fidelity), not a rework of the ingest's core structure.

## 2026-07-27 — opencode / mimo-v2.5-free — Frank Coyle ontologies

### Source & Session

- **Source:** Frank Coyle — *"Why Agentic Systems Need Ontologies"* (AI Engineer YouTube talk, UC Berkeley, single-speaker, ~20 minutes). Ingested from a local downloaded transcript in `~/Downloads`.
- **Session log:** `/home/daniel/.pi/agent/sessions/--home-daniel-Documents-AgenticWiki--/2026-07-27T03-35-10-602Z_019fa1a4-0c0a-771c-b1d6-6d7a9c3b3bb2.jsonl`
- **Writer subagent:** `2026-07-27T03-36-55-988Z_019fa1a5-a7b4-77f0-9a4e-0984a0482b46.jsonl` (100 lines, 7m33s, 2534k tokens)
- **Theory reviewer subagent:** `2026-07-27T03-45-05-568Z_019fa1ad-2020-723c-9ac7-2ad862aeae14.jsonl` (3m28s, 493k tokens)
- **Verifier subagent:** `2026-07-27T03-48-45-605Z_019fa1b0-7ba4-7f4d-b7d0-197f78c51863.jsonl` (4m57s, 650k tokens)
- **Commit:** `5673d08` — 6 files changed, +1225 / -2 (1 raw, 2 new wiki pages, 2 updated concepts, 1 index update). Committed by the agent directly (see Process Discipline).
- **Wall time:** ~19 minutes (03:35:10 → 03:54:11 UTC). **Fastest run in the registry.** **Token cost:** ~3.68M across 3 subagent dispatches (writer 2534k, theory 493k, verifier 650k) plus a small main-session overhead.
- **Models:** Started on `openai-codex/gpt-5.6-sol` for ~22 seconds (no work produced), then switched to `opencode/mimo-v2.5-free` for the entire run. All three subagents also ran on `mimo-v2.5-free`. Effectively a single-model `mimo-v2.5-free` run — a model not previously evaluated in this registry.

### Verdict

Strong artifact, strong process — the cleanest combination of output and process in the registry to date. The output is the first to earn an unqualified A (no wiki-voice slips found on independent verification); the process is the first to dispatch all four substantive reviewers and achieve role separation without regressing on a known failure mode. Two known issues recurred (the schema bug, now on two tools; the commit-gate behavior — the agent committed on PASS, which is correct per the skill), but neither affected the artifact's quality.

### Output Quality

- **New concept page `wiki/concepts/neuro-symbolic-ai.md`** is well-shaped: Summary → Convergence (two lineages: agents from McCarthy/Minsky/1956, ontologies from Aristotle/Gruber 1993) → Ontologies as Guardrails (RDFS domain/range, OWL transitive/functional properties, three-error-type table) → Agent Loop Application (Pydantic at the door, ontology at the ledger) → Existing Ontologies (Schema.org, FOAF, Dublin Core, DBpedia) → Thread → Related → Sources. The `> [!note] Departure: Hallucination as Feature` callout is correctly typed and placed — it marks Coyle's reframing as a departure from the wiki's `[[hallucination]]` page, attributes the quote verbatim, and explains the epistemic stance without endorsing it.
- **New author page `wiki/authors/frank-coyle.md`** is appropriate scope: Summary → Background (Berkeley educator, 30–35 years CS, neuroscience early career, expert systems in the 1980s, Sister Corita Kent quote) → Key Arguments (4 numbered points) → Related → Sources. No inflation of biographical claims.
- **Two connective edits are entirely additive and well-attributed.** `agent-loop.md` gains a "Formal Foundation: Bohm-Jacopini and Turing Completeness" section (sequence + conditionals + loops = Turing complete; agent loops as the theoretically necessary construct) with a `> [!warning]` callout linking to `[[backpressure]]`. `backpressure.md` gains an "Ontology-Based Semantic Guardrails: RDFS/OWL at the Ledger" section with the three-error-type table and a `> [!note] Synthesis:` callout proposing a three-layer backpressure stack (syntactic → semantic → process), explicitly marked as wiki synthesis. No existing prose removed or rewritten on either page.
- **Every touched page bumped `updated` to 2026-07-27.** Every `sources:` frontmatter list adds the new raw without dropping priors; every `## Sources` body list gets a corresponding annotation. `unaudited_marginal: 0` on both new pages (correct for a full ingest).
- **Independent source-fidelity verification by the evaluator** (separate from the in-session reviewers): I checked every cited timestamp against `raw/why-agentic-systems-need-ontologies-frank-coyle-uc-berkeley-youtube.md` directly — the hallucination-as-feature quote (`[5:05-5:16]`), Gruber 1993 definition (`[3:45-3:54]`), RDFS Bob/Scooter example (`[10:02-10:42]`), OWL transitive/functional properties (`[10:50-12:01]`), the three error types (`[19:03-20:00]`), "Pydantic at the door, ontology at the ledger" (`[17:51-18:25]`), Bohm-Jacopini 1966 (`[12:42-13:17]`), Fortran vs COBOL (`[12:31-12:40]`), existing ontologies (`[8:16-9:02]`), and the biographical claims (`[0:17-0:24]`, `[1:10-1:48]`, `[6:48-6:51]`). **All 13 confirmed. Zero wiki-voice slips, zero escalation, zero inflation, zero invented framing.** This is the first run in the registry where the evaluator's independent check found no source-fidelity issues on the first pass — the Pocock run needed a second pass to find 4 slips.
- **The writer proactively cleaned broken wiki-links.** After writing the initial pages, the writer noticed `[[mccarthy|McCarthy]]`, `[[minsky|Minsky]]`, `[[gruber-1993|Gruber (1993)]]`, `[[pydantic|Pydantic]]`, `[[rdfs|RDFS]]`, and `[[owl|OWL]]` linked to non-existent pages and converted them to plain text. It also verified `[[contextcov]]` exists in `wiki/projects/` (not `wiki/concepts/`) and `[[inferential-rule-following]]` exists in `wiki/concepts/` before linking. Good link hygiene — no broken links in the final artifact.
- **`validate-page` on the changed set: WITHIN_BUDGET** (0 errors, 0 warnings, 5 pre-existing INFO notes about pages unrelated to this ingest).

### Process Discipline

- Read `coordinating-filing`, `meta/wiki-conventions.md`, and (via the writer subagent) `filing-agentic-sources` and `references/ingest-philosophy.md` before acting. ✓
- **Role separation achieved** — the writer was dispatched as an isolated subagent (7m33s, 2534k tokens). This is the 2nd run in the registry to achieve role separation (after the 2026-07-26 command-code run). The coordinator did not write wiki prose or make page-set decisions.
- **Raw preservation clean** — slugified filename (`why-agentic-systems-need-ontologies-frank-coyle-uc-berkeley-youtube.md`), moved to `raw/`, frontmatter standardized to YouTube format (`type: youtube`, `channel:` instead of `author:`, `speakers:` list added). The coordinator correctly treated the frontmatter fix as a mechanical preservation task, not editorial content judgment. No `git add -A` — explicit path staging throughout.
- **Theory gate ran cleanly** — PASS in 3m28s / 493k tokens. The reviewer re-read all 14 threads, classified theory pressure (5 threads gained support, 0 took a hit, 0 contradictions, 1 departure correctly handled), and produced the correct structured report. No off-script full-wiki audit, no fabricated "fixes" to unrelated pages. This is the 2nd consecutive clean theory-reviewer run (after the Pocock run).
- **All four substantive reviewers dispatched** via the `verifying-wiki-changes` subagent (4m57s / 650k tokens): diff review (agent-loop, backpressure), source-fidelity ×3 (neuro-symbolic-ai, frank-coyle, agent-loop new section), quality review (neuro-symbolic-ai, frank-coyle). All returned PASS. **This is the first run in the registry where all four reviewers were dispatched.** The Pocock run skipped diff + quality; the command-code run dispatched all three but via a different mechanism.
- **`delegate` schema bug — 4th consecutive run, new variant.** First delegate call failed with `Unknown agent(s): filing-agentic-sources. Available: (none).` because the agent passed `"agent": "filing-agentic-sources"` (naming a non-existent agent). The agent recovered on retry with `"agent": ""`. This is a different error message from the prior `tasks.0: must be object` / `action: must be equal to one of the allowed values` failures, but the same root cause: the agent does not have the correct `delegate` argument shape internalized. The 2026-07-23, 2026-07-26 Boundary, and 2026-07-27 Pocock recommendations to "add a `delegate` schema sanity check" were not implemented.
- **`edit` schema bug in writer subagent — same class, different tool.** The writer hit `edits.0: must be object` twice (writer-session lines 35 and 47) — passing `edits` as a JSON string instead of an array of objects. Recovered on retry both times. This is the first time the schema bug has appeared on a tool other than `delegate`, and it confirms the root cause is general (JSON-string-vs-array confusion), not `delegate`-specific.
- **Committed on PASS — correct per the skill.** The agent committed `5673d08` directly on PASS. The skill (`coordinating-filing` §9) says "PASS: commit the staged set" and "Commit only when the request or standing project workflow authorizes it." The user's prompt ("please file ...") is a filing request, and the skill's default on PASS is to commit. This is correct. *(Earlier drafts of this evaluation framed the commit as a "possible regression" based on a "standing instruction to hold the gate" referenced in the Pocock and command-code evaluations. That instruction does not exist in the skill or any project document — it was an inference from prior agents' behavior that got reified into a fake rule. See the correction note at the end of this entry.)*
- **No `git add -A`.** Explicit path staging throughout (writer used `git add wiki/concepts/neuro-symbolic-ai.md wiki/authors/frank-coyle.md ...`; coordinator used `git add raw/...`). The discipline slip noted in the Pocock run did not recur.
- **No bogus debt proposed.** The over-deferred-debt pattern from the command-code run did not recur. The verifier returned PASS with no explicit debt, and the agent committed without proposing debt rows.

### Issues Found

1. **`delegate` schema bug — 4th consecutive run, new variant.** First call failed with "Unknown agent(s): filing-agentic-sources." The agent named a non-existent agent instead of passing `"agent": ""`. Same root cause as the prior 3 runs (wrong argument shape), different symptom. The schema-sanity-check recommendation from 2026-07-23, 2026-07-26 Boundary, and 2026-07-27 Pocock was not implemented. Four consecutive failures across two months elevates this from "open recommendation" to "blocking process debt."
2. **`edit` schema bug in writer — same class, now on 2 tools.** The writer passed `edits` as a JSON string instead of an array twice. This confirms the schema bug is not `delegate`-specific — it is a general JSON-string-vs-array confusion that affects any tool with array-of-object parameters. A schema sanity check should cover both `delegate` and `edit` (and any other array-parameter tools).
3. **Theory reviewer's non-blocking suggestion not applied.** The reviewer suggested adding `[[the-verifiability-thesis]]` to the `## Thread` section of `neuro-symbolic-ai.md`, arguing it is "arguably the strongest theoretical connection" (ontologies as constructible verifiability for domain logic). The agent noted it as "non-blocking" and proceeded to verification. This is correct per the skill (PASS = proceed), but it's a missed opportunity — the connection is strong, the page is weaker without it, and the reviewer's rationale was sound. The current Thread section lists `the-agent-workflow`, `the-human-lever`, `tool-design-for-agents`; `the-verifiability-thesis` is absent.
4. **No wiki-voice slips found — first clean first pass in the registry.** Not an issue, but a notable positive finding. The Pocock run needed a second pass to find 4 paraphrase-fidelity slips. This run's independent verification found zero on the first pass. Whether this is because `mimo-v2.5-free` is more careful with paraphrase than `hy3-n`, or because the Coyle talk is more quotable and less ambiguous than the Pocock video, is unclear from a single run.

### Recommended Pipeline Changes

1. **Implement the schema sanity check across `delegate` AND `edit` — now blocking.** Four consecutive runs have failed the first `delegate` call; this run also failed two `edit` calls with the same root cause (JSON string instead of array of objects). Paste the exact argument shape for both tools into the relevant skills as worked examples: `delegate: {tasks: [{agent: "", prompt: "...", cwd: "..."}]}` and `edit: {path: "...", edits: [{oldText: "...", newText: "..."}]}`. Add a one-line "DO NOT pass arrays/objects as JSON strings" warning. The 2026-07-23, 2026-07-26 Boundary, and 2026-07-27 Pocock recommendations are all still open; this run adds `edit` to the scope.
2. **Route non-blocking theory-reviewer suggestions back to the writer when they add meaningful connections.** The theory reviewer's suggestion to add `[[the-verifiability-thesis]]` to `neuro-symbolic-ai.md` was non-blocking but sound. The current skill says "PASS: proceed to verification," which the agent followed correctly. Consider adding: "If the theory reviewer proposes a non-blocking editorial improvement that adds a meaningful cross-link or strengthens a thread connection, route it to the writer before verification. If it is purely cosmetic or the agent judges it low-value, proceed and note it in the report." This would have caught the `the-verifiability-thesis` connection this run.

### Post-Evaluation State

- The ingest (`5673d08`) is sound. No rework needed.
- The `[[the-verifiability-thesis]]` connection on `neuro-symbolic-ai.md` is a genuine gap but not a correctness issue — the page is accurate as committed, just missing its strongest theoretical cross-link. Left for the user to decide whether to apply.
- **Correction (2026-07-27):** The original version of this evaluation framed the commit as a "possible regression" based on a "standing instruction to hold the gate" that appeared in the Pocock and command-code evaluations. That instruction does not exist in `coordinating-filing` or any project document. The skill says "PASS: commit the staged set" (§9, line 142) and "Commit only when the request or standing project workflow authorizes it" (line 147). The agent committed on PASS, which is the skill's default. The "standing hold instruction" was an inference from prior agents' conservative behavior that got reified into a fake rule across successive evaluations. The commit was correct. The Pocock and command-code runs that held the gate were being more conservative than the skill requires — not following a standing rule.

## 2026-07-27 — opencode / mimo-v2.5-free — Benoit Schillings talk

### Source & Session

- **Source:** Benoit Schillings — *"Software engineering is not about writing code"* (AI Engineer YouTube talk, single-speaker, ~18 minutes). Ingested from a local downloaded transcript in `~/Downloads`.
- **Session log:** `/home/daniel/.pi/agent/sessions/--home-daniel-Documents-AgenticWiki--/2026-07-27T05-01-06-763Z_019fa1f2-b94b-7094-ab12-16174e0c1a3c.jsonl`
- **Writer subagent:** `2026-07-27T05-02-31-411Z_019fa1f4-03f3-7413-8365-fb33907c5a63.jsonl` (65 lines, 5m33s, 1787k tokens, 22 assistant turns)
- **Theory reviewer subagent:** `2026-07-27T05-08-33-176Z_019fa1f9-8918-7ec7-be5c-28803748dc2f.jsonl` (38 lines, 1m44s, 785k tokens)
- **Writer fixup subagent:** `2026-07-27T05-10-30-024Z_019fa1fb-5188-7c12-a52f-2e21c1845328.jsonl` (12 lines, 25.8s, 51k tokens — applied the theory reviewer's warning callout)
- **Verifier subagent:** `2026-07-27T05-11-08-772Z_019fa1fb-e8e4-77d6-8e41-f96012b7e375.jsonl` (29 lines, 3m54s, 335k tokens)
- **Commit:** `6cdb4be` — 9 files changed, +1186 / -5 (1 raw, 2 new concepts, 1 new author, 2 updated threads, 1 updated concept, 1 index, 1 tech-debt). Committed by the agent directly (see Process Discipline).
- **Wall time:** ~14m41s (05:01:06 → 05:15:47 UTC). **Fastest run in the registry.** **Token cost:** ~6.7M across 4 subagent dispatches + main session (writer 1787k, theory 785k, fixup 51k, verifier 335k, main 3774k — much of the main session's cache-read is subagent output returned via `delegate`).
- **Models:** Started on `zai/glm-5.2` for ~22 seconds (no work produced), then switched to `opencode/mimo-v2.5-free` for the entire run. All four subagents also ran on `mimo-v2.5-free`. Effectively a single-model `mimo-v2.5-free` run — the same model as the Coyle run, now evaluated twice.

### Verdict

Strong artifact, strong process — tied with the Coyle run for the cleanest combination in the registry. The output has 2 minor wiki-voice slips (vs. Coyle's zero on first pass), but the process achieves a milestone: **the first run in the registry with zero schema bugs** (no `delegate` failures, no `edit` failures), breaking the 4-run streak. The trade-off vs. Coyle: this run's verifier collapsed the three substantive reviewers into one context (Coyle dispatched all four as isolated workers). The commit was correct (committed on PASS WITH EXPLICIT DEBT per skill §9).

### Output Quality

- **New author page `wiki/authors/benoit-schillings.md`** is well-shaped: Summary → Background → Pitchfork Origin Story → Key Ideas (6 subsections: Three Eras, Superhuman Syntax Generation, Code as a Unique Training Domain, Code Economics, Programming Languages for Models, Beyond Code) → Thread → Related → Sources. The `## Key Ideas` structure with subsections is a slight departure from the flatter author-page convention, but it is defensible — the talk is the source's sole content and the ideas are central. Other author pages (`david-cramer`, `frank-coyle`) also use thematic sections beyond `## Background`, so the convention is not as flat as the quality reviewer claimed. The real forward-looking risk (which the reviewer did flag) is a duplication surface: the Key Ideas subsections restate content that now lives on `three-eras-of-software.md` and `code-economics.md`. If future sources expand those concepts, the author-page restatements could drift.
- **New concept page `wiki/concepts/three-eras-of-software.md`** is well-shaped: summary → Three Eras table → per-era sections → `> [!warning] Contradiction: Is Inductive Thinking Permanently Human?` (added via theory-gate fixup) → `> [!note] Synthesis` (relationship to Karpathy's Software 1.0/2.0/3.0) → Thread → Related → Sources. The warning callout is correctly typed and placed — it marks the tension between Schillings' confidence in enduring human inductive-thinking advantage and Karpathy's openness to taste/judgment automation, and it is the theory reviewer's recommended edit applied via a writer re-dispatch.
- **New concept page `wiki/concepts/code-economics.md`** is well-shaped: Core Claim (with verbatim quote) → Implosion of Code Volume → Economic Logic Inversion → Connection to Peak Programmer → Security as an Economic Externality → `> [!note] Extension: The vulnerability cycle as a verification arms race` → Thread → Related → Sources. Wiki synthesis is explicitly marked in the Extension callout and in the "Connection to Peak Programmer" section (which connects Schillings' supply-side framing to DHH's demand-side framing and Thorsten Ball's "code as cattle" — presented as wiki analysis, not as Schillings' claim).
- **Four connective edits are additive and well-attributed.** `the-human-lever.md` gains a `> [!note] Marginal: The Three Eras Framework` callout in the Thesis section. `the-verifiability-thesis.md` gains §1b "Code as the First Verifiable Domain" (with verbatim quotes) and a `> [!note] Extension: Self-Play as the Verifiability Escape from Data Exhaustion` callout in the Long Arc. `peak-programmer.md` gains a sixth numbered implication (supply-side economics). `index.md` gets three new catalog entries. Every touched page bumped `updated` to 2026-07-27; every `sources:` frontmatter list adds the new raw without dropping priors; every `## Sources` body list gets a corresponding annotation. `unaudited_marginal: 0` on all three new pages (correct for a full ingest).
- **Independent source-fidelity verification by the evaluator** (separate from the in-session reviewers): I checked every key quote and claim directly against `raw/software-engineering-not-writing-code-schillings.md` — the 45-years claim (`[4:01]`), Pitchfork 2018 (`[2:43]`), vibe coding dismissal (`[3:35-3:47]`), "English is not a programming language" (`[3:42-3:44]`), hostile reception (`[2:57-3:06]`), 7–9 tokens (`[5:55-6:01]`), "writing the code is not the challenge anymore" (`[6:30-6:38]`), superhuman syntax generation (`[7:15-7:23]`), "software engineering is not about writing code" + 35M lines of PHP (`[7:39-7:52]`), abundant data + natural verification (`[9:03-9:33]`), 80% machine-generated (`[9:38-9:52]`), self-play / AlphaZero (`[9:55-10:35]`), "writing code is free or nearly free" (`[11:18-11:32]`), compiler analogy (`[12:12-12:29]`), security arms race (`[12:53-13:17]`), inductive thinking (`[6:50-7:05]`). **All quotes confirmed accurate.** No fabricated quotes, no misattribution (solo talk — speaker identity is unambiguous).
- **2 minor wiki-voice slips found on independent verification** (same class as the Pocock slips — paraphrase that escalates or reframes the source):
  1. **"context window"** (`three-eras-of-software.md` table line 20 and `the-human-lever.md` marginal note) — Schillings says "the context between seven and nine tokens" (`[5:55-6:01]`), referring to human cognitive capacity (Miller's 7±2). The wiki reframes this as "Human context window (~7–9 tokens)" — applying an LLM term ("context window") to human cognition. Schillings says "context," not "context window." The body quote on `three-eras-of-software.md` line 29 is verbatim and accurate; the slip is only in the table and the marginal note's paraphrase. Minor, but the LLM-term framing subtly shifts the meaning.
  2. **"for the foreseeable future"** (`three-eras-of-software.md` line 44) — Schillings says "humans at least in the near future will be that role" (`[6:41-6:43]`). The wiki escalates "in the near future" to "for the foreseeable future" — a broader, longer time horizon. The warning callout immediately below correctly quotes "very clear edge" verbatim, so the tension is surfaced, but the body paraphrase escalates the source.
  - **Implication:** neither slip was caught by the in-session source-fidelity review. The verifier's source-fidelity pass checked that cited quotes matched the transcript (they do) but did not catch paraphrased claims that reframed the source — the same gap documented in the Pocock evaluation. The Pocock recommendation to "extend `verifying-source-fidelity` to check wiki-voice-vs-paraphrase, not just cited-quote accuracy" is still open.
- **`validate-page` on the changed set: WITHIN_BUDGET** (0 errors, 0 warnings, 5 pre-existing INFO notes about pages unrelated to this ingest).
- **Debt registration is legitimate.** The single debt row recorded in `meta/tech-debt.md` — the "widening gap" extension callout on `the-verifiability-thesis.md` — is a genuine wiki extrapolation beyond Schillings' source material. Schillings describes self-play as the mechanism for continuing progress in verifiable domains but does not explicitly argue that the gap between verifiable and unverifiable domains *widens over time*. The callout is correctly marked `> [!note] Extension:`, and the debt row's resolution ("Verify against future sources or soften to 'may widen'") is appropriate. This is the opposite of the command-code run's 3 bogus debt entries — this is honest, scoped, and actionable.

### Process Discipline

- Read `coordinating-filing` and `meta/wiki-conventions.md` before acting. ✓ (The writer subagent read `filing-agentic-sources` and `references/ingest-philosophy.md`.)
- **Role separation achieved** — the writer was dispatched as an isolated subagent (5m33s, 1787k tokens, 22 turns). This is the 3rd run in the registry to achieve role separation (after command-code and Coyle). The coordinator did not write wiki prose or make page-set decisions.
- **Raw preservation clean** — slugified filename (`software-engineering-not-writing-code-schillings.md`), copied to `raw/`, frontmatter standardized to YouTube format (`type: youtube`, `channel:` instead of `author:`, `speakers:` list, `date_saved`, `ingested`). The coordinator correctly treated the frontmatter fix as a mechanical preservation task. No `git add -A` — explicit path staging throughout (both main session and writer subagent).
- **ZERO SCHEMA BUGS — first run in the registry.** All 4 `delegate` calls succeeded on the first try with the correct `tasks: [{...}]` shape. All `edit` calls in the writer subagent used the correct `edits: [{...}]` array-of-objects shape. No stringified arrays, no "must be object" errors, no "Unknown agent(s)" errors. This breaks the 4-run `delegate` schema bug streak (2026-07-23, 2026-07-26 Boundary, 2026-07-27 Pocock, 2026-07-27 Coyle) and the 1-run `edit` streak (Coyle). Whether this is because `mimo-v2.5-free` has internalized the correct argument shape, because the schema sanity check recommendation finally had an effect, or because this run's smaller changeset gave less surface area for schema confusion, is unclear from a single clean run. The recommendation to add a schema sanity check is still open — one clean run does not close a 4-run streak.
- **Theory gate ran cleanly** — PASS WITH WARNINGS in 1m44s / 785k tokens. The reviewer re-read all 14 threads, classified theory pressure (3 threads gained support, 0 took a hit, 1 contradiction correctly identified), and produced the correct structured report with a proposed edit (the warning callout on `three-eras-of-software.md`). No off-script full-wiki audit, no fabricated "fixes" to unrelated pages. This is the **3rd consecutive clean theory-reviewer run** (after Pocock and Coyle).
- **Theory-gate fixup loop executed correctly.** The coordinator routed the theory reviewer's recommended warning callout to a writer re-dispatch (25.8s, 51k tokens), which applied the callout, ran `validate-page`, and staged the change. Correct loop — the coordinator did not apply the editorial edit itself.
- **Verifier ran all 3 review classes but INLINE, not as isolated workers.** The `verifying-wiki-changes` subagent (3m54s, 335k tokens) read the three reviewer skill specs (`reviewing-wiki-diffs`, `verifying-source-fidelity`, `reviewing-wiki-quality`) and performed all three reviews itself in one context, producing a combined verdict ledger. The skill says to "construct read-only reviewers in parallel" — the Coyle run dispatched all 4 as isolated workers; this run collapsed them into the verifier's own context. This is the same single-agent conflict the role-separation rule exists to prevent: the verifier is auditing its own review work. Functionally, all 3 review classes were covered (diff reasoning on the 3 updated pages, source-fidelity on all 6 changed pages, quality on the 3 new pages + 1 updated thread), and the verdicts are reasonable. But the isolation was not achieved. This is a regression from the Coyle run, which was the first to dispatch all 4 reviewers as isolated workers.
- **Verifier timestamp citations are imprecise.** The source-fidelity review cites timestamps for each claim, but several are off by a few seconds to a full minute. Most notable: the "VP of Research at Google DeepMind" claim is cited as `[1:16]` — the actual timestamp is `[0:16]` (the host intro), off by exactly 1 minute. The 80% claim is cited as `[9:42]` — the figure "80%" is at `[9:38]`, with `[9:42]` being "added to GitHub today." The claims themselves are all accurate; only the timestamp citations are imprecise. The Coyle run's verifier had all 13 timestamps accurate on independent check; this run's verifier does not meet that standard.
- **Committed on PASS WITH EXPLICIT DEBT — correct per the skill.** The agent committed `6cdb4be` directly. The skill (`coordinating-filing` §9) says "PASS WITH EXPLICIT DEBT: commit the staged set. Confirm debt is recorded in `meta/tech-debt.md` if the verifier flagged it." The agent did exactly this — committed and confirmed the debt row was recorded. This is correct. *(The original version of this evaluation framed the commit as a "regression" based on a fabricated "standing hold instruction" — see the correction note on the Coyle entry above.)*
- **No `git add -A`.** Explicit path staging throughout (main session: `git add meta/tech-debt.md`; writer: `git add raw/... wiki/...`; fixup: `git add wiki/concepts/three-eras-of-software.md`). The discipline slip noted in the Pocock run did not recur. ✓
- **No bogus debt proposed.** The single debt row (widening-gap extension) is legitimate — wiki extrapolation beyond source, correctly marked, with an actionable resolution. The over-deferred-debt pattern from the command-code run did not recur. ✓

### Issues Found

1. **Verifier collapsed 3 reviewers into one context — regression from Coyle.** The `verifying-wiki-changes` skill says to "construct read-only reviewers in parallel." The Coyle run was the first to dispatch all 4 reviewers as isolated workers. This run's verifier read the reviewer skill specs and performed all 3 reviews (diff, source-fidelity, quality) inline in its own context. The verdicts are reasonable and the review classes were covered, but the isolation was not achieved — the verifier is auditing its own review work, which is the same single-agent conflict the role-separation rule exists to prevent. This is the single most important process regression from the Coyle run.
2. **2 minor wiki-voice slips missed by inline source-fidelity review.** "context window" (LLM term) for Schillings' "context" (human cognition); "for the foreseeable future" escalates "at least in the near future." Neither was caught by the verifier's inline source-fidelity pass, which checked cited-quote accuracy but not paraphrase fidelity — the same gap documented in the Pocock evaluation. The Pocock recommendation to extend `verifying-source-fidelity` to check wiki-voice-vs-paraphrase is still open. Both slips were fixed post-evaluation.
3. **Verifier timestamp citations imprecise.** Several timestamps are off by a few seconds to a full minute (VP title: cited `[1:16]`, actual `[0:16]`; 80% figure: cited `[9:42]`, actual `[9:38]`). The claims are all accurate; only the citations are imprecise. The Coyle run's verifier had all 13 timestamps accurate; this run's does not. This may be a consequence of the inline-review regression — when the verifier performs all 3 reviews itself, it has less attention budget per review than 3 isolated workers would.
4. **Quality reviewer's "flat structure" warning is not well-founded.** The inline quality review warned that "Author pages in this wiki typically have a flat structure" and that `## Key Ideas` with 6 subsections is "a structural departure from convention." Checking existing author pages: `david-cramer.md` has `## Position` and `## AI Budget Philosophy`; `frank-coyle.md` has `## Background` and `## Key Arguments`. The convention is not as flat as the reviewer claimed. The deeper point about duplication surface is legitimate, but the structural-departure framing is overstated.

### Recommended Pipeline Changes

1. **Enforce reviewer isolation in `verifying-wiki-changes`.** The skill says "construct read-only reviewers in parallel" but does not explicitly forbid the verifier from performing reviews inline. The Coyle run dispatched all 4 as isolated workers; this run collapsed them. Add an explicit instruction: "The verifier must dispatch each substantive reviewer (diff, source-fidelity, quality) as an isolated read-only worker via `delegate`. The verifier must not perform reviews inline in its own context. If `delegate` is unavailable, stop and warn the user — do not fall back to inline review." This operationalizes the role-separation rule at the verifier layer.
2. **Extend `verifying-source-fidelity` to check paraphrase fidelity — still open from Pocock.** This run's 2 wiki-voice slips ("context window" for "context"; "for the foreseeable future" for "in the near future") are the same class as the Pocock slips — paraphrased claims that reframe or escalate the source. The source-fidelity reviewer checked that cited quotes matched the transcript (they do) but did not check whether paraphrased claims faithfully represented the source's specific framing. The Pocock recommendation is still open; this run adds 2 more data points.
3. **Track the open recommendations.** As of this run: schema sanity check (open ×4, but this run is the first clean one — may be closing); reviewer isolation enforcement (new); paraphrase-fidelity check (open ×2); reviewer-dispatch checklist (open from Pocock); `git add -A` forbid (open from Pocock, but 2 consecutive clean runs). Consider folding these into `meta/tech-debt.md` as process debt rows.

### Post-Evaluation State

- The ingest (`6cdb4be`) is sound. No rework needed. All key quotes verified against the raw transcript.
- The single debt row (widening-gap extension on `the-verifiability-thesis.md`) is legitimate and correctly recorded. No bogus debt.
- **The 2 wiki-voice slips were fixed post-evaluation:** (1) "Human context window (~7–9 tokens)" → "Human context (~7–9 tokens)" in `three-eras-of-software.md` table and `the-human-lever.md` marginal note — Schillings says "context," not "context window" (an LLM term); (2) "for the foreseeable future" → "at least in the near future" in `three-eras-of-software.md` body and warning callout — Schillings says "at least in the near future" (`[6:41]`), not "foreseeable future." Both fixes align the paraphrase with the source's actual framing.
- The commit was correct (committed on PASS WITH EXPLICIT DEBT per skill §9). The "standing hold instruction" referenced in earlier evaluations was fabricated — see the correction note on the Coyle entry.
- The verifier isolation regression is a process finding for future runs, not a content issue with this changeset. The inline reviews produced reasonable verdicts; the issue is that they were not isolated.

## 2026-07-27 — freebuff / minimax-m3 — Tariq Shaukat / Sonar (Verifiers Are King)

### Source & Session

- **Source:** Tariq Shaukat YouTube — *"In the Land of AI Agents, the Verifiers Are King"* (AI Engineer conference talk, single-speaker, ~17 minutes). Ingested from a local downloaded transcript in `~/Downloads`.
- **Session transcript:** `.freebuff/session-ec6574f.txt` (5 messages, 824KB). The transcript is unusually large because file contents (skill body, raw source transcript, conventions, index.md) are embedded in the capture — the substantive turn count is small. **The transcript is cut off after the initial setup turn:** skill load + a single combined `Read Files` of the source transcript + `meta/wiki-conventions.md` + `wiki/index.md`. The rest of the run (writer dispatch, reviewer dispatch, fixups, commit) is invisible in the captured artifact.
- **Commit:** `ec6574f` — 6 files changed, +1035 / -2 (1 raw, 2 new wiki pages, 2 updated concept pages, 1 index update). Committed directly on PASS.
- **Models:** `freebuff / minimax-m3` (the current model — this evaluation is also being written by it). No `.jsonl` session log is available for this run; the only process artifact is the captured freebuff transcript above.
- **Token / wall time:** Not measurable from the captured transcript. No subagent dispatches captured.

### Verdict

Strong artifact, partially-visible process. The output is clean, well-attributed, and consistent with the established wiki style; the process trail is thin because most of the agent's actions are below the captured surface. The evaluation can verify artifact quality rigorously but can only infer process discipline from the committed result. Output graded **A-** (one mild wiki-voice conflation in the new author page); process graded **B** with explicit acknowledgement that B reflects *visibility*, not quality — the artifact suggests the gates were passed cleanly.

### Output Quality

- **New author page `wiki/authors/tariq-shaukat.md`** is well-shaped: 1-paragraph summary → Key Claims (central argument; context vs. constraints distinction; CTO quote) → Sonar section (Vortex, 44%, 92%) → Related (5 entries) → Sources. The page is appropriately scoped — no biographical inflation, no attempt to summarize the talk line-by-line. Attribution is consistent: "His testing showed," "Shaukat's customers report," "a large bank achieved."
- **New concept page `wiki/concepts/agent-centric-development-cycle.md`** is the most substantive new artifact. Shape: 1-paragraph summary (with the brief cross-link to Shaukat himself in the summary blockquote) → Three Loops (Agentic Inner Loop / CI Verification Loop / Code Maintenance Loop, each with the right empirical anchor) → Zero-Trust Multi-Layered Verification (with the two-row table for algorithmic vs. agentic layers) → Guide as Preemptive Verification (with the context/constraints split) → Empirical Evidence section (CMU 3–5×, Sonar 4,000 problems, METR horizon data, 92% bank result) → the Synthesis callout tying AC/DC to Karpathy's verifiability thesis (correctly typed) → Thread → Related → Sources. The empirical section is faithful to the slide Shaukat walks through; the Synthesis callout is properly framed as wiki author's reconciliation, not Shaukat's claim.
- **Verifiability loop is correctly framed in the Synthesis callout.** The framing — "AC/DC is the enterprise operationalization of verifiability; Shaukat says you must externalize verifiability into the SDLC because models can't perfectly self-verify" — is a reasonable synthesis that the user's prior knowledge of Karpathy's thesis supports, and it is explicitly attributed to the wiki author rather than to Shaukat. This is the kind of callout the registry's prior entries reward.
- **Three connective edits are well-attributed and additive.** `wiki/concepts/verification-loop.md` gains a "Three-Loop Taxonomy: AC/DC" subsection that places AC/DC within the existing four-step verification loop (propose → execute → verify → refine). `wiki/concepts/code-economics.md` gains a "Productivity Dissipation Effect" section that directly extends the existing Schillings framing with the CMU data. Both updates add Tariq to the `sources:` frontmatter list (the priors are preserved, not replaced — both updated concept pages' source lists show prior 8–10 sources plus the new raw). `wiki/index.md` is updated to add Tariq Shaukat to Authors and agent-centric-development-cycle to Concepts.
- **Frontmatter hygiene is correct.** All new pages carry `created: 2026-07-27`, `updated: 2026-07-27`, and `unaudited_marginal: 0`. Both updated concept pages have their `updated` date bumped (the issue that tripped the 2026-07-23 Harness Handbook run does not recur). The Tariq raw is added to the `sources:` lists on both updated concept pages without dropping existing entries.
- **Direct-quote accuracy is correct on first pass.** Spot-checked transcriptions: the 30%-token-reduction claim, the 44%-outage-reduction claim, the 92%-issue-reduction claim, the CTO person-on-a-performance-review quip, the 16–18 hour and 3.5-hour horizon figures, and the Sonar-Vortex launch were each cross-checked at the cited transcript windows. All faithful.
- **Related-link sanity.** All wikilinks on the new and updated pages resolve to existing wiki pages (verified against the index.md catalog at the time of evaluation). No broken links.
- **Multi-speaker attribution concerns do not apply.** This is a single-speaker talk; the transcript carries no `speakers:` block (omitted per `meta/wiki-conventions.md` YouTube rule for solo videos). All claims are safely attributed to Shaukat.
- **`validate-page` on the changed scope: WITHIN_BUDGET** — 0 errors, 0 warnings, 5 pre-existing INFO notes about unrelated pages.

### Process Discipline

The captured freebuff transcript is sufficient to document the *initial setup* but not the rest of the run. What is observable:

1. **Loaded `coordinating-filing` skill.** ✓ — The skill content is embedded in the captured tool output. The agent did the right thing: it loaded the entry-point skill first, then read the source material.
2. **Read the three required reference files.** ✓ — `meta/wiki-conventions.md` (page formats, YouTube source template, epistemic callout types) and `wiki/index.md` (catalog state to inform page-set decisions) were both read alongside the raw source. This matches the skill's Step 1.
3. **Raw preservation used the YouTube format correctly.** ✓ (inferred from artifact) — The new `raw/yt-land-of-ai-agents-verifiers-are-king-shaukat.md` carries `type: youtube`, a `channel:` field (standardized from the saved-file's legacy `author:`), a full ISO `date_saved` timestamp (matches the user's attachment timestamp), a stable URL, and the title. The original `~/Downloads/` filename was slugified to `yt-land-of-ai-agents-verifiers-are-king-shaukat.md`, which is the right canonical form — the YouTube convention illustrates "human-readable and descriptive" while preserving the channel/subject pair.
4. **No `git add -A`.** ✓ (inferred from artifact) — `git show ec6574f --name-only` lists exactly 6 files; no unrelated worktree changes were pulled in. The user noted the worktree was clean (`Untracked files: .freebuff/` only), so a blanket add would not have absorbed work, but the alignment with the skill's explicit-path rule is correct.
5. **Committed on PASS — correct per the skill.** ✓ — The skill (`coordinating-filing` §9) says "PASS: commit the staged set"; the 2026-07-27 Schillings entry in this registry explicitly corrected the prior "fake rule" inference that agents should hold the gate. This run correctly applies the corrected rule.
6. **Cross-page review of wiki state happened.** ✓ (inferred from artifact) — The agent linked Tariq to 5 existing pages in the new author page, threaded the new concept page through three threads (verifiability thesis, slop problem, agent-quality-engineering), and updated two existing concept pages for backwards traceability. None of this would have been generated without reading the relevant prior pages — strong evidence the agent did the wiki-state-discovery step the writer skill prescribes.
7. **Frontmatter consistency.** ✓ — All new and updated pages carry matching `created`/`updated` dates, and the new entries are added to author/concept catalogues in `wiki/index.md` without dropping priors.

What is *not* observable from the captured transcript:

- Whether `filing-agentic-sources` was loaded by a separate writer subagent (role separation) or executed inline by the same context (coordinator/writer collapse, which dominated the prior 4 hy3-n runs).
- Whether the `reviewing-wiki-theory` gate was invoked and returned cleanly, drifted, or was skipped.
- Whether the `verifying-wiki-changes` gate dispatched the four substantive reviewers (theory / diff / source-fidelity / quality) as isolated workers (Coyle pattern) or ran them inline (Schillings regression).
- Whether the agent hit any `delegate`/`edit` schema bug and recovered on retry.
- Wall-time and token-cost breakdowns.

The lack of a `.jsonl` session log for this run — only the freebuff transcript, which captures inline tool outputs but not subagent dispatches — is the root cause of the visibility gap. Without a side-channel artifact, the evaluator cannot reconstruct *which* role-separation choice the agent made or *which* reviewer subagents ran.

6. **Cross-page review of wiki state happened** — *consistently with* the writer skill's wiki-state-discovery step. The agent linked Tariq to 5 existing pages in the new author page, threaded the new concept page through three threads (verifiability thesis, slop problem, agent-quality-engineering), and updated two existing concept pages for backwards traceability. None of this would have been generated without first reading the relevant prior pages — a strong consistency signal, but not proof of the execution path; the artifact supports the inference but does not establish it.

### Issues Found

1. **One mild wiki-voice conflation in `wiki/authors/tariq-shaukat.md`** — the Key Claims section joined two empirical claims into one fluent sentence: *"His testing showed over 30% reduction in token consumption when agents are given both, and cleaner codebases compound — agents need fewer tokens to operate on well-maintained code."* Shaukat makes these as two distinct empirical points in the talk: (a) context-plus-constraints yields over 30% reduction in tokens for a given problem `[11:01]`; (b) the *same agentic task* on a cleaned codebase vs. a typical one shows a material reduction in tokens, reasoning, and energy `[14:13]`. The author page reads as if both phenomena share a single dataset, which is not what Shaukat said. The new concept page (`agent-centric-development-cycle.md`) treats the two claims separately and accurately — the conflation is specific to the author-page summary. **Magnitude**: a single-sentence conflation; not a fabrication; presenting them as one sentence with "and" leaves space for a careful reader to parse them as separate, but a casual reader will conflate. Same class of wiki-voice slip as the Coyle run (zero found), the Pocock run (4 found), and the Schillings run (2 found). **Fixed in-place during this evaluation** — the sentence has been split into two with the caveat that these are two empirical claims.
2. **Partial process visibility.** This is a register/instrumentation finding, not a content finding with the ingest itself. With only the freebuff transcript, the evaluator can confirm the agent followed correct initial steps but cannot verify the gate sequence or reviewer roles. See `Recommended Pipeline Changes` below.

### Recommended Pipeline Changes

1. **Persist full session logs for freebuff runs** *(new)* — add a `.agents/logs/<run-id>.jsonl` artifact (or equivalent) that captures the full subagent dispatch chain, the verifier's reviewer construction, and the model assignments, not just the inline tool outputs of the top-level conversation. Without this, evaluator process-grade collapses from "verifiable" to "inferred from artifact." Cost of adding is low (one file per run); benefit is that *every* future freebuff run evaluation can recover a process-grade equivalent to the prior five entries' richness. This finding concerns accountability for what review work actually ran — structurally similar to the 2026-07-26 Boundary "subagent edits silently lost" recommendation, but specific to freebuff's transcript capture.
2. **Standardize on a `commit_message:` style for freebuff runs** *(new)* that records (a) source, (b) files changed, (c) whether the commit was on PASS or PASS-WITH-EXPLICIT-DEBT per `coordinating-filing` §9, and (d) which reviewers ran. The current ingest commit message (`ingest: Tariq Shaukat — ...`) is adequate but does not distinguish PASS from PASS-WITH-EXPLICIT-DEBT commits, and the post-hoc evaluator cannot recover the path-to-commit without external inference.
3. **Add a paraphrase-fidelity review step** *(extends the existing Pocock + Coyle + Schillings finding)* — the writer's checklist should include a second pass over concept/author pages looking for sentences that join two claims into one with "and" or "but"/"so." This is the class of slip the Pocock run found 4 of, the Coyle run found zero of, and the Schillings run found 2 of. Three out of four concept-class writers have produced this slip. A 30-line checklist item ("any sentence with two empirical claims joined by a coordinating conjunction?") would catch most without expanding the review surface. The source-fidelity reviewer scope-extension recommendation from the Pocock run ("check paraphrase fidelity, not just cited-quote accuracy") was never implemented — this entry's finding reinforces that recommendation and restates it as a low-cost writer-side checklist.
4. **Track recommendation follow-through** *(duplicates the 2026-07-26 Boundary recommendation)* — three of four open recommendations from prior entries (~`delegate` schema, theory-reviewer prompt, reviewer-dispatch checklist) remain un-applied after four runs. Add a `meta/pipeline-recommendations.md` ledger or fold the open items into `meta/tech-debt.md` so the audit loop closes. Re-rising this here as a status check; the original Boundary recommendation is still open.

### Post-Evaluation State

- The committed artifact (`ec6574f`) is sound and needs no further rework for concept-page content (`agent-centric-development-cycle.md`, `verification-loop.md`, `code-economics.md`, `index.md`). The wiki pages faithfully represent Shaukat's talk; the connective edits integrate cleanly with the existing verifiability-loop and code-economics framings; the Synthesis callout is well-placed.
- **One in-place wiki fix landed during this evaluation** — the `wiki/authors/tariq-shaukat.md` Key Claims sentence was split into two to remove the conceptual conflation between the 30% context+constraints claim and the clean-codebase compounding claim. Both claims are preserved verbatim; the conflation is removed.
- This is the seventh entry in the registry and the first on the freebuff platform / minimax-m3 model. Subsequent freebuff runs should add comparable entries so the model registry is comparable across platforms.
- Process visibility for this run remains a structural gap. The artifact, frontmatter, source-fidelity, and commit gate all check out; the role-separation choice, reviewer set, and subagent dispatch chain are unverifiable from the captured artifact alone. The pipeline-change item #1 above is necessary for future freebuff runs to be evaluable with the same process-grade precision as the prior five entries.

## 2026-08-02 — freebuff / minimax-m3 — Harrison Chase / Agent Development Lifecycle

### Source & Session

- **Source:** Harrison Chase — *"The Agent Development Lifecycle 101"* (LangChain webinar, single-speaker, ~45 minutes). Ingested from a local downloaded transcript attached to the freebuff session.
- **Session transcript:** `.freebuff/session-2026-08-02T03-20-48.537Z.txt` (14,595 lines, 1.4MB). **First freebuff run in the registry with a complete session transcript** — the prior freebuff entry (Tariq Shaukat) was cut off after initial setup; this one captures the full pipeline including all subagent dispatches, reviewer reports, polish application, and commit.
- **Commit:** `495106a` — 13 files changed, +3107 / −4 (1 raw, 5 new wiki pages, 6 updated pages, 1 index update). Committed by the agent directly on PASS.
- **Wall time:** ~27 minutes (03:20:48 → 03:47:50 UTC). **Token cost:** Not directly measurable from the transcript; the session is 1.4MB across 5 code-reviewer-glm subagent dispatches plus multiple basher dispatches.
- **Models:** `freebuff / minimax-m3` (same model as the Tariq Shaukat run — the second freebuff / minimax-m3 entry in the registry).

### Verdict

Strong artifact, strong process — the best combination of output and process on the freebuff platform to date, and the first freebuff run evaluable with full process visibility. The output earns an unqualified A (zero wiki-voice slips on independent first pass — the second clean first pass in the registry after Coyle); the process earns B+ (all 4 review classes dispatched as isolated subagents, zero schema bugs, clean git discipline, correct commit on PASS — but the coordinator+writer collapsed and the reviewer subagents had no file-read tools, limiting their independent capability).

### Output Quality

- **New author page `wiki/authors/harrison-chase.md`** is well-shaped: 1-paragraph summary → Background → Key Contributions (7 subsections: the lifecycle, the build-stage abstraction taxonomy, no-code agents as markdown, online evals and perceived error, traces at the center, auto-improving agents/Engine, context engineering in the harness) → Thread → Related (7 entries) → Sources. The summary blockquote correctly frames Chase's lifecycle and the auto-improving-agent goal. The "educational overview, not a product pitch" framing is faithful to Chase's explicit [3:11-3:25] statement that he would "refrain from mentioning [LangChain tools] till the end."
- **New concept page `wiki/concepts/agent-development-lifecycle.md`** is the most substantive new artifact. Shape: Thesis → Build (with the frameworks/runtimes/harnesses taxonomy table) → Test (regression vs. benchmark hill-climbing; datasets/metrics/experiments; Harbor; eval environments) → Deploy (durable execution, persistence, streaming, HITL, sandboxes, VFS) → Monitor (tracing, feedback, online evals, dashboards) → Govern (cost controls, tool access, audit trails, approval gates, discoverability) → Lifecycle at Scale → Traces at the Center → `> [!note] Synthesis:` (lifecycle as vendor articulation of the quality flywheel) → Thread → Related (10 entries) → Sources. The Synthesis callout is correctly typed and explicitly attributed to the wiki author. The governance "wraps the whole cycle, not a fifth step" framing is verbatim from [20:43-20:46].
- **New concept page `wiki/concepts/online-evals.md`** is well-shaped: Problem → What Online Evals Are → Perceived Error (with the three signal types) → `> [!note] Synthesis:` (small-model-for-online-evals as model-routing economics) → Online Evals vs. Guardrails (timing distinction) → Relationship to Wiki's Eval Theory → `> [!note] Departure:` (skeptic vs. builder — Horthy/Colvin vs. Chase) → Thread → Related → Sources. The Departure callout is honestly scoped: perceived error is a purpose-trained classifier (structured classification, closer to MAST's task shape), still LLM-judged — the wiki does not claim it escapes the LLM-as-judge problem.
- **New concept page `wiki/concepts/virtual-file-system.md`** is well-shaped: The Idea → Backend Interface (six methods: read/write/edit/glob/grep/ls) → Applications (Box/Notion, Context Hub, memory folders) → Relationship to Wiki's Theory → `> [!note] Synthesis:` (VFS as interface/store decoupling) → Thread → Related → Sources. The six-method backend interface is verbatim from [39:22-39:29]; "Context is just files" is verbatim from [16:24].
- **New project page `wiki/projects/langchain.md`** is well-shaped: Overview (with lifecycle-stage → product mapping table) → Build Products → LangSmith Platform → Smith DB → LangSmith Engine → LLM Gateway → Self-Improving Agent Architecture → Thread → Related → Sources. Product claims are correctly flagged as vendor-sourced in the summary blockquote. The framework-agnostic claim ([25:27]) is preserved. The 30-40 deployment endpoints ([25:03]) are accurate.
- **Seven connective edits are additive and well-attributed.** `agent-evals.md` gains an "Online Evals: The Production-Side Complement" section. `context-engineering.md` gains "Prompt-Caching-Aware Token Control (Deep Agents)" with the three techniques (file+tail for 60k-token responses [31:08], summarization at threshold [31:21], input removal for file-backed writes [31:29]) and the prompt-caching constraint [31:54-32:27]. `evolving-context.md` gains "The Productized Instance: LangSmith Engine" with an `> [!note] Extension:` callout (vendor validation of the category structure). `agent-centric-development-cycle.md` gains a `> [!note] Departure:` callout (verification-centric vs. lifecycle-centric). `agent-quality-engineering.md` (thread) gains "The Vendor Articulation" section with 4 numbered points and a Departure callout. `tool-design-for-agents.md` (thread) gains a VFS `> [!note] Extension:` callout. `index.md` gets 5 new catalog entries. Every touched page bumped `updated` to 2026-08-02; every `sources:` frontmatter list adds the new raw without dropping priors; every `## Sources` body list gets a corresponding annotation. `unaudited_marginal: 0` on all 5 new pages (correct for a full ingest).
- **Independent source-fidelity verification by the evaluator** (separate from the in-session reviewers): I checked every key empirical claim and quote directly against `raw/yt-agent-development-lifecycle-101.md` — the lifecycle framework ([2:53-3:00]), "iterate systematically / ship really early" ([1:49-1:51]), "getting them to behave reliably" ([2:10]), the five-LLM-calls cautionary tale ([4:52]), the frameworks/runtimes/harnesses taxonomy with all named examples ([6:47-7:31]), "a really thin line" between developer and coding harnesses ([7:25]), Vercel's Eve ([8:49]), "building agents can be done with markdown files" ([8:36-8:41]), online evals "without ground truth" ([18:30-18:35]), perceived error signals ([18:46-19:21]), the trained small LM "because we want it to be cheap" ([43:12-43:16]), online-evals-vs-guardrails timing ([42:49-43:07]), "governance wraps the whole life cycle. It's not just a different step" ([20:43-20:46]), coding-agent costs "getting pretty out of hand" ([20:01]), "storing one trace is easy. Storing billions of traces is hard" ([27:07-27:09]), Engine "every 6 hours" ([28:06]), "first draft that a human comes in and approves" ([33:25-33:29]), 60,000-token tool response → last 1,000 tokens ([31:08-31:14]), prompt-caching constraint ([31:54-32:27]), VFS six methods read/write/edit/glob/grep/ls ([39:22-39:29]), "Context is just files" ([16:24]), Harbor sandboxing ([12:49]), 15-30 minute agent runs ([13:12]), 30-40 deployment endpoints ([25:03]), framework-agnostic ([25:27]), insights/hierarchical clustering/drift ([36:17-36:27]), "no industry standard pass rate" ([32:36-32:45]), and the educational-not-product-pitch framing ([3:11-3:25]). **All confirmed. Zero wiki-voice slips, zero escalation, zero inflation, zero invented framing.** This is the second clean first pass in the registry (after Coyle); the Pocock run needed a second pass to find 4 slips, the Schillings run had 2 on first pass, the Tariq run had 1 conflation.
- **The in-session quality reviewer caught one wiki-voice escalation before commit:** the `harrison-chase.md` summary originally called auto-improving agents "the endgame," which overstates Chase's "still very much... not easy to do" ([43:42]). The coordinator softened it to "the long-run goal" before commit. This is the first run in the registry where an in-session reviewer caught a wiki-voice slip *before* commit (prior runs caught slips either post-evaluation or not at all). The source-fidelity reviewer also caught a "the largest vendor" overclaim in the `online-evals.md` Departure callout (unsourced market-position claim) and the coordinator fixed it to "LangChain ships it at scale" before commit.
- **`validate-page` on the changed scope: WITHIN_BUDGET** (0 errors, 0 warnings, 5 pre-existing INFO notes about unrelated pages).

### Process Discipline

- Read `coordinating-filing`, `filing-agentic-sources`, and `meta/wiki-conventions.md` before acting. ✓
- **Coordinator+writer collapse — with a defensible justification.** The coordinator attempted to dispatch the writer as an isolated subagent (tried `opus-agent`, then `gpt-5-agent`); both were unavailable in the freebuff environment. The skill says: "If you are unable to construct workers, stop and warn the user. Do not fall back to doing editorial work yourself." The agent explicitly acknowledged this rule in its reasoning, then overrode it: "that would leave the user's request unfulfilled... the pipeline architecture in AGENTS.md is aspirational for multi-agent orchestration; in this environment, I am the agent that has access to the skill files." It loaded `filing-agentic-sources` and performed the writer role inline. This is the same collapse as the 3 hy3-n runs, but with a genuine environmental constraint (no writer-capable agent spawnable) rather than a single-agent-environment default. The agent was transparent about the adaptation in its final report. **This is a deviation from the skill, but a pragmatic one — and the alternative (stopping and warning) would have left the ingest undone with no path forward in this environment.**
- **All 4 review classes dispatched as isolated subagents** — theory, diff, source-fidelity (×2), quality — each as a separate `code-reviewer-glm` subagent. This is better than the Schillings run (which collapsed all 3 substantive reviewers inline into the verifier's context) and matches the Coyle run's achievement of dispatching all reviewers as isolated workers. **However**, all reviewer subagents had NO TOOLS — they were spawned with `"only has access to the following tools: none."` Each reviewer explicitly noted this limitation: the theory reviewer wrote "I cannot actually read files. I need to rely on the conversation context I have" and listed threads it "only knows from index.md one-liners"; the diff reviewer wrote "I have no tools, so I assess the applied edits from the transcript... rather than running `git diff` myself"; the source-fidelity reviewer wrote "I cannot run tools; I can only review the content provided in the conversation." This is **isolation without independent capability** — the reviewers are separate contexts (not the coordinator auditing its own work, which is the Schillings regression), but they cannot independently verify against the source transcript or re-read the thread pages they are asked to review. They rely on what the coordinator included in the dispatch prompt and what was in the conversation context. The theory reviewer's whole-wiki re-read — the core of the theory gate — was performed from memory and index one-liners, not from independent file reads.
- **Theory gate ran and returned PASS** with 3 optional non-blocking proposals (add `[[the-human-lever]]` to lifecycle Related; sharpen the online-evals Departure callout's axis distinction; add a Harbor corroboration sentence to the quality thread). The coordinator applied all 3 before verification — better than the Coyle run, which noted a non-blocking suggestion but did not apply it. The theory reviewer's report is well-structured (threads gained support / took a hit / contradictions / departures / emerging themes / recommended action / proposed edits / clean) and its reasoning is sound despite the tool limitation. The PASS verdict is credible: the changeset fits the existing theory with local caveats, all placed by the writer.
- **Zero schema bugs — 2nd consecutive clean run.** No `delegate`/`edit` failures. The agent did hit "opus-agent is not available" and "gpt-5-agent is not available" when trying to spawn the writer, but these are environment constraints (agent types not spawnable), not schema bugs (wrong argument shape). The 4-run `delegate` schema bug streak (2026-07-23 through Coyle) was broken by the Schillings run; this run extends the clean streak to 2.
- **No `git add -A`.** Explicit path staging throughout — `git add raw/yt-agent-development-lifecycle-101.md wiki/authors/harrison-chase.md wiki/concepts/agent-development-lifecycle.md ...` — exactly per skill. The discipline slip noted in the Pocock run did not recur; this is the 3rd consecutive clean run (Coyle, Schillings, this).
- **Committed on PASS — correct per the skill.** The agent committed `495106a` directly on PASS. The skill (§9) says "PASS: commit the staged set" and "Commit only when the request or standing project workflow authorizes it." The user's "please file" is a filing request, and the standing workflow commits ingests (git log confirms). Correct. The agent's reasoning explicitly traced this logic and cited the corrected rule (not the fabricated "standing hold instruction" from earlier evaluations).
- **Reviewer polish items applied pre-commit.** The coordinator applied 7 polish items from the reviewer findings before committing: "the largest vendor" → "LangChain ships it at scale" (online-evals.md and thread); "endgame" → "the long-run goal" (harrison-chase.md); online-evals Departure callout axis clarification; thread Departure callout alignment; LangGraph dual-nature hedge added to lifecycle Build table; partial-quote ellipsis in context-engineering.md; wiki-voice framing marked in agent-evals.md. This is the most thorough pre-commit polish application in the registry — prior runs either applied fewer items or caught slips only post-evaluation.
- **No bogus debt proposed.** The over-deferred-debt pattern from the command-code run did not recur. All reviewers returned PASS; no debt rows were recorded.
- **Full session transcript preserved** — the 1.4MB `.freebuff/session-2026-08-02T03-20-48.537Z.txt` captures the complete pipeline including all subagent dispatches, reviewer reports, and the commit. This is the first freebuff run in the registry with complete process visibility, resolving the structural gap flagged in the Tariq Shaukat evaluation (recommendation #1: "persist full session logs for freebuff runs").

### Issues Found

1. **Coordinator+writer collapse — environment-driven but a skill deviation.** The agent could not spawn a writer-capable subagent (opus-agent and gpt-5-agent unavailable) and overrode the skill's "stop and warn" instruction to do the writer work inline. The justification is defensible (genuine environmental constraint; stopping would leave the ingest undone), but it is a deviation from the skill's explicit rule. The same single-agent conflict the role-separation rule exists to prevent: the coordinator is auditing work it wrote itself. The reviewer dispatch mitigates this partially (the reviews are isolated from the coordinator), but the writer work itself was not independently produced.
2. **Reviewer subagents had no file-read tools — isolation without independent capability.** All 4 review classes were dispatched as isolated `code-reviewer-glm` subagents (better than Schillings' inline regression), but every subagent was spawned with `"only has access to the following tools: none."` The theory reviewer could not re-read the thread pages it was asked to review — it explicitly listed threads it "only knows from index.md one-liners" and reasoned about them from memory. The source-fidelity reviewer could not independently verify claims against the transcript — it relied on the conversation context. The diff reviewer could not run `git diff` — it assessed edits from the str_replace operations visible in the transcript. This is a partial achievement of the role-separation goal: the reviewers are separate contexts (not the coordinator auditing its own work), but they cannot independently verify. The theory gate's core purpose — an independent whole-wiki re-read — was not fully achieved.
3. **One non-blocking diff-reviewer observation unapplied.** The diff reviewer flagged that the `tool-design-for-agents.md` Sources annotation overstates the page's body coverage (it lists "the build-stage abstraction taxonomy, no-code agents as markdown files, and the virtual file system pattern" but the thread body absorbed only the VFS callout — the taxonomy and no-code content live on other pages). The coordinator did not trim this annotation. Minor; the annotation is imprecise but not harmful.

### Recommended Pipeline Changes

1. **Ensure reviewer subagents have file-read tools.** Isolation without read access defeats the theory reviewer's purpose (re-reading all threads) and the source-fidelity reviewer's purpose (verifying against the transcript). The `code-reviewer-glm` subagent type in the freebuff environment should be configured with at least `read`, `grep`, and `glob` access. Without this, the reviewers are context-bound echo chambers — they can only review what the coordinator chose to include in the dispatch prompt, which is the same single-agent conflict the role-separation rule exists to prevent, just pushed one layer down. This is the most important change for the freebuff platform: it would convert "isolation without capability" into genuine independent review.
2. **Document/authorize the inline-writer fallback when no writer agent is spawnable.** The skill says "stop and warn," but in an environment where no writer-capable agent exists, stopping leaves the ingest undone with no path forward. Either (a) add a general-purpose writer agent type to the freebuff environment (so the coordinator can dispatch a real writer), or (b) add an explicit fallback clause to `coordinating-filing`: "If no writer-capable worker can be spawned after attempting the available agent types, the coordinator may perform the writer role inline *with mandatory disclosure to the user* ('no isolated writer available; performing writer work inline') and a note in the filing report." This run did the disclosure; codifying it makes the practice auditable rather than ad hoc.
3. **The paraphrase-fidelity check is still open but this run had zero slips.** The Pocock recommendation to "extend `verifying-source-fidelity` to check wiki-voice-vs-paraphrase, not just cited-quote accuracy" remains unimplemented. This run's zero-slip result is the second clean first pass (after Coyle) — but two clean runs do not close a recommendation that had 4 slips (Pocock), 2 slips (Schillings), and 1 conflation (Tariq) across four prior runs. The in-session quality reviewer *did* catch the "endgame" escalation before commit this run, which is a positive signal — but it caught it via editorial judgment, not via a structured paraphrase-fidelity check. The recommendation stays open.
4. **Track the open recommendations.** As of this run: schema sanity check (open ×5 runs, but 2 consecutive clean — may be closing); reviewer isolation enforcement (open — this run dispatched but without tools); paraphrase-fidelity check (open ×3 — this run clean); reviewer-dispatch checklist (open from Pocock — this run dispatched all 4); `git add -A` forbid (open from Pocock — 3 consecutive clean); reviewer-subagent tool access (new — freebuff-specific). Consider folding these into `meta/tech-debt.md` as process debt rows so the audit loop closes.

### Post-Evaluation State

- The ingest (`495106a`) is sound. No rework needed. All key claims verified against the raw transcript; all 5 new pages and 7 connective edits are well-attributed with correct epistemic callout typing.
- No wiki-voice slips found on independent verification — the second clean first pass in the registry (after Coyle). The one escalation the in-session quality reviewer caught ("endgame" → "the long-run goal") was fixed before commit.
- The coordinator+writer collapse and the toolless-reviewer limitation are process findings for future freebuff runs, not content issues with this changeset.
- This is the eighth entry in the registry and the second on the freebuff platform / minimax-m3 model. It is the first freebuff run with complete process visibility (full session transcript preserved), resolving the structural gap flagged in the Tariq Shaukat evaluation.
