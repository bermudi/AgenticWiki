# Pipeline Recommendations

This is the durable queue for **process** recommendations from filing evaluations, audits, and verification reviews. `meta/tech-debt.md` tracks page/source evidence debt; this ledger tracks changes to the workflow that produces and checks wiki artifacts.

A recommendation is not closed merely because a skill was edited. Keep it `open` until the implementation is present **and** the stated closure evidence has been observed in a later run. AG-000 is the explicit bootstrap exception: creating the durable ledger is itself its closure event. Retain closed rows so repeated recommendations have a visible history instead of evaporating into evaluation prose.

## Status contract

- `open` — the recommendation is unresolved, or its implementation has not yet been demonstrated by the closure evidence.
- `closed` — the implementation is present and the closure evidence has been recorded in this row.

The filing coordinator owns ledger updates. When a run tests a recommendation, update its implementation/evidence columns in the same changeset as the filing report or audit note. Every closure record must cite the observation date and at least one durable commit, session, or run identifier; prose such as “observed later” is not closure evidence. Add recurring process recommendations here rather than burying them in content-debt rows.

## Ledger

| ID | Status | Recommendation | Origin / recurrence | Current implementation | Closure evidence |
|---|---|---|---|---|---|
| AG-000 | closed | Maintain a durable process-recommendation ledger. | Boundary and subsequent evaluations repeatedly recommended a ledger after open items were carried forward without an owner. | This file is the authoritative process queue; the filing coordinator owns updates, and the queue is linked from `AGENTS.md`, the coordinating skill, and the audit skill. | **Bootstrap closure:** 2026-08-03, run `agenticwiki-workflow-hardening-2026-08-03`, base commit `71ccdd1`; ledger, ownership, status rules, and retained history established in the current changeset. |
| AG-001 | open | Every required verifier review must run in a fresh, isolated, read-only worker; never collapse diff, source-fidelity, or quality review into the verifier's own context. If worker dispatch is unavailable, stop and warn. | Benoit Schillings evaluation: three reviewers were performed inline and two wiki-voice slips escaped. | `verifying-wiki-changes` now makes isolated dispatch a hard gate. | A fully observed run shows one independent worker per required review, with read/search access and no inline fallback. |
| AG-002 | open | Add an explicit paraphrase-fidelity / wiki-voice pass: check whether the source supports the specific framing, not merely the topic. | Matt Pocock: four slips; Benoit Schillings: two; Tariq Shaukat: one conflation. | `verifying-source-fidelity` now checks actor, action, modality, scope, intensity, and source-specific framing for paraphrases. | A later independent source-fidelity pass demonstrates the check on paraphrased claims and the run has no unresolved drift. |
| AG-003 | open | Treat reviewer filesystems as non-authoritative. Reviewers return reports or diffs; the coordinator routes accepted fixes to the shared-checkout writer, inspects the resulting diff/staging, and re-verifies. Never trust a reviewer's self-reported “fixed” status. | Boundary evaluation: a theory worker claimed unrelated fixes that never reached the coordinator's checkout. | `coordinating-filing` now requires writers to share the authoritative checkout, keeps reviewers isolated/report-only, and defines the fix/reverification loop. | A dated, identified run observes the full loop: reviewer proposal, authoritative-checkout writer fix, targeted deterministic check, and affected reviewer rerun. |
| AG-004 | open | When using Pi, use its tool schemas exactly: `delegate` takes a top-level `tasks` array of task objects; `edit` takes a top-level `path` plus an `edits` array of `{oldText,newText}` objects. Never pass arrays/objects as JSON strings or invent agent names. Other harnesses use native adapters. | Delegate schema failures recurred across five AgenticWiki runs; Coyle also exposed the `edit` variant. | `coordinating-filing` now scopes copyable examples to a Pi adapter while keeping workflow topology harness-neutral. | Two consecutive dated, identified Pi runs complete with zero `delegate`/`edit` schema-validation failures after this documentation landed. |
| AG-005 | open | Keep theory review report-only and in scope. Unrelated cleanup is an out-of-scope observation, not a self-authorized fix. | Boundary evaluation: theory reviewer drifted into an unrelated full-wiki audit and reported fixes that did not land. | `reviewing-wiki-theory` now has an explicit **DO NOT EDIT** boundary and an `Out-of-scope observations` report section; the coordinator's filesystem-isolation rule requires proposed edits to be routed and re-applied deliberately. | Two observed theory gates return the required theory report without unrelated edits or self-reported fixes. |
| AG-006 | open | Isolated reviewers must have genuine read/search capability; isolation without repository access is only an echo chamber. | Harrison Chase evaluation: reviewer workers had no tools and could not independently re-read the files. | Verifier dispatch guidance now requires read/search capabilities; the worked `delegate` example uses `tools: ["ro"]`. | A run's reviewer ledger and dispatch records show each worker could read the repository and independently cite files. |
| AG-007 | open | Preserve enough dispatch/process telemetry to make role separation auditable, especially for harnesses whose transcript capture is partial. | Tariq Shaukat evaluation: process quality could not be established from the artifact-only transcript. | `coordinating-filing` now requires the final report to record each worker/task, reviewer read-only capability, dispatch result, retry/empty-result events, and fallback/intervention when exposed. | A complete observed run records the dispatch chain, reviewer identities/capabilities, and verdict ledger without relying on artifact inference. |
| AG-008 | open | Enforce explicit-path staging; never use `git add -A` in the filing pipeline. | Pocock and later evaluations recorded repeated staging-discipline slips despite the skill prohibition. | The coordinating and verifier skills require exact staging-command telemetry when exposed and forbid inferring command history from final staged-state parity. | Two dated, identified runs expose exact `git add -- <path>` commands and no `git add -A` invocation; a run without command telemetry cannot close this row. |
| AG-009 | open | Route meaningful non-blocking theory-reviewer suggestions to the writer before verification when they add a useful connection or strengthen a thread. | Coyle evaluation: the meaningful `the-verifiability-thesis` cross-link suggestion was not routed before the gate. | `coordinating-filing` now routes such suggestions, records the writer's disposition, and reruns theory when an applied edit materially changes the theory picture. | A full ingest shows a meaningful suggestion routed to the writer, with apply/decline disposition recorded before verification. |
| AG-010 | open | Use write-capable workers sharing the coordinator's authoritative checkout; if none is available, use the disclosed inline-writer fallback. Never rely on isolated/non-authoritative writer edits, and keep reviewer roles isolated. | Chase evaluation: the coordinator performed writer work inline after worker types were unavailable, but the exception was only disclosed ad hoc. | `coordinating-filing` defines the authoritative writer topology, mandatory fallback disclosure, scope, and reviewer prohibition. | A dated, identified worker-unavailable filing records the disclosure, reason, inline-written pages, and still completes isolated reviewer dispatch before the gate. |
